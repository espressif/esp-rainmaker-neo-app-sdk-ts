# SPDX-FileCopyrightText: 2024-2026 Espressif Systems (Shanghai) CO LTD
#
# SPDX-License-Identifier: Apache-2.0

openapi: 3.0.0
info:
  title: ESP RainMaker Neo Platform API
  version: 0.1.0
  description: |
    REST API for the ESP RainMaker Neo platform - everything except
    sign-up and authentication, which live in the separate
    **ESP RainMaker Neo User API** (`User_Api_Swagger.yaml`).

    Covers what an app or admin dashboard does once a user is signed
    in: groups and sharing, node association, control surfaces
    (schedules, automations, triggers, time-series), admin operations
    (bulk node registration, voice-assistant integrations), and the
    credential exchange used for SigV4-signed requests.

    Each endpoint declares its security scheme:

    - `CognitoAuthorizer` - a bearer access token in the `Authorization`
      header, from the ESP User OIDC issuer for end users or the admin
      Cognito pool for admins. Used only by `/v1/user/credentials`, which
      also needs the paired `id_token` in the body.
    - `gvaAccessToken` - an ESP User `access_token` issued to the
      voice-assistant client during Google account linking, validated by the
      Lambda. Used only by `/v1/integrations/gva`.
    - `sigv4` - AWS SigV4 signing using the temporary credentials
      returned by `POST /v1/user/credentials`. Used by every other
      endpoint.

    Endpoints are organised by tag. End-user-facing surface is listed
    first (Group Management, Sharing, Node Association, control,
    telemetry, Matter, Notification Endpoints); admin-only surface
    follows (Node Administration, Integrations, File Management,
    Google Voice Assistant).
  termsOfService: http://swagger.io/terms/
  contact:
    email: esp-rainmaker-admin@espressif.com
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html

# A `servers:` block lets Swagger UI (and the docusaurus-openapi-docs
# Try-it-out panel) populate a base URL. Replace `{deployment}` with
# your stack's API Gateway hostname; `{stage}` is the API Gateway stage,
# which every RMNG deployment creates as `prod`.
servers:
  - url: https://{deployment}/{stage}
    description: Your ESP RainMaker Neo deployment's API Gateway base URL
    variables:
      deployment:
        default: api.example.com
        description: Replace with your API Gateway hostname.
      stage:
        default: prod
        description: API Gateway stage name.

# Top-level tag declarations - each operation `tags:` field references
# one of the names below. Order here is the order rendered in
# Swagger UI sections and in the docs-site sidebar (when
# `groupPathsBy: tag` is enabled). End-user-facing groups first,
# admin/deployment groups after the comment.
tags:
  - name: User Credentials
    description:
      Exchange the login's token pair for temporary AWS credentials used to
      sign every other (sigv4) call. Mandatory step right after login.
  - name: Role Assumption
    description:
      Step up to credentials scoped to a specific group, or to the IoT-user role
      required to open MQTT WebSocket connections. Not needed for plain REST
      calls.
  - name: Group Management
    description:
      Create, list, update, and delete groups and subgroups; manage memberships.
  - name: Group Sharing
    description: Share an entire group with another user.
  - name: Subgroup Sharing
    description: Share a subgroup specifically.
  - name: Sharing Requests
    description: Accept, reject, and list pending share invitations.
  - name: Node Association
    description:
      Associate a physical node with a group; covers both classic and Matter
      flows.
  - name: Node Configuration
    description:
      Read the device-reported configuration (type, params, services) of a node.
  - name: Node Tags
    description: Read and update user-scoped tags on a node.
  - name: Group Automation
    description: Define and run automation rules at the group level.
  - name: Node Schedule
    description: Schedule parameter updates on a node.
  - name: Node Trigger
    description: Event-based triggers for nodes.
  - name: Node Timeseries
    description: Query historical and aggregated parameter data.
  - name: Matter Fabric
    description: Generate Matter NOCs and other Matter-specific operations.
  - name: Notification Endpoints
    description:
      Per-user operations called from end-user apps - register a delivery
      endpoint on an integration (push install, account-link, etc.).
  - name: Assisted Claiming
    description:
      Cloud-signed node onboarding - reserve a server-assigned node ID for a
      device MAC, then exchange a CSR for a certificate signed by the
      deployment's claiming CA. Present only on deployments that enable
      claiming (indicated by the rmng-claim-core AssistedClaiming output being
      present in the published outputs).
  # --- Admin surface below ---
  - name: Node Administration
    description:
      Admin-only - bulk node registration, single-node registration jobs.
  - name: File Management
    description:
      Admin-only - upload templates and CSV files used by registration jobs.
  - name: Integrations
    description: Admin-only - configure 3p integrations (Alexa Skill, etc.).
  - name: Google Voice Assistant
    description: Admin-only - configure the Google Voice Assistant integration.

paths:
  /v1/groups/{groupId}/node-assoc-requests:
    post:
      summary: Create a node association request
      description: |
        Initiates the process of associating a node with a user's group.
        Generates a request ID and challenge string that will be used in the verification step.

        For Matter-capable groups, the challenge is 32 bytes (64 hex characters) which serves
        as the CSR nonce for device NOC generation.

        For non-Matter groups, the challenge is a standard alphanumeric string.
      tags:
        - Node Association
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      responses:
        "201":
          description: Association initiated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InitiateResponse"
              examples:
                nonMatterGroup:
                  summary: Non-Matter group response (alphanumeric challenge)
                  value:
                    request_id: "abc123def456"
                    challenge: "xK9mPq2sT7vW4yB8"
                matterGroup:
                  summary:
                    Matter group response (32-byte hex challenge used as CSR
                    nonce)
                  value:
                    request_id: "abc123def456"
                    challenge: "A1B2C3D4E5F6A1B2C3D4E5F6A1B2C3D4E5F6A1B2C3D4E5F6A1B2C3D4E5F6A1B2"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          description: Group not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Failed to store association data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "Failed to store data"

  /v1/groups/{groupId}/node-assoc-requests/{requestId}/verify:
    post:
      summary: Verify node association
      description: |
        Verifies the node association. Two authentication methods are supported, and exactly
        one must be used (they are mutually exclusive):

        **Method 1: challenge_response (traditional flow)**
        - The node signs the SHA256 hash of the challenge using its private key.
        - Requires `node_id` to identify the node whose certificates will be used for verification.
        - Works for both Matter and non-Matter groups.
        - The node is immediately added to the group upon successful verification.
        - No NOC is generated - use nocsr_elements if NOC generation is needed.

        **Method 2: nocsr_elements (Matter attestation flow)**
        - The device provides NOCSRElements TLV containing CSR, nonce, and optional vendor_reserved1.
        - Only valid for Matter-capable groups.
        - Requires `attestation_challenge` and `attestation_signature`.
        - If vendor_reserved1 contains a nodeID with registered certificates, signature is verified.
        - For pure Matter nodes (no vendor_reserved1 or no certificates), a random node_id is generated.
        - The `node_id` field is optional/ignored - extracted from vendor_reserved1 or auto-generated.
        - Returns the device NOC (Node Operational Certificate) and Matter Node ID.
        - The node is NOT added to the group yet - call /confirm after device installs the NOC.

        **For non-Matter groups:**
        - Use challenge_response method only (nocsr_elements not supported).
        - The node is immediately added to the group upon successful verification.
        - Returns a success response.

        **For Matter-capable groups (two options):**
        - Option A: Use challenge_response to add node to group without NOC generation.
        - Option B: Use nocsr_elements to get a NOC, then call /confirm to complete association.
      tags:
        - Node Association
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyRequest"
            examples:
              challengeResponseNonMatter:
                summary: "Non-Matter group: challenge_response flow"
                description: |
                  The node signs the SHA256 hash of the challenge using its private key.
                  Requires node_id. The node is immediately added to the group.
                value:
                  challenge_response: "304502210098f3e3b2a1c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f502207a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5"
                  node_id: "MySmartLight01"
              challengeResponseMatter:
                summary:
                  "Matter group: challenge_response flow (RainMaker device, no
                  NOC generation)"
                description: |
                  Same as non-Matter flow. The node is added to the group directly
                  without NOC generation. Use this when the device already has
                  operational credentials and only needs group membership.
                value:
                  challenge_response: "304502210098f3e3b2a1c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f502207a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5"
                  node_id: "MySmartLight01"
              nocsrElementsWithNodeId:
                summary:
                  "Matter group: nocsr_elements flow (device with registered
                  certificates)"
                description: |
                  The device provides NOCSRElements TLV containing CSR, CSR nonce,
                  and vendor_reserved1 (containing the nodeID/thing name).
                  The attestation signature is verified against the device's registered
                  certificates. Returns a device NOC. Call /confirm after the device
                  installs the NOC.
                value:
                  nocsr_elements: "1530012930820122300d06092a864886f70d01010105000382010f003082010a0282010100b5c2030201023020"
                  attestation_challenge: "0102030405060708090a0b0c0d0e0f10"
                  attestation_signature: "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2"
              nocsrElementsPureMatter:
                summary:
                  "Matter group: nocsr_elements flow (pure Matter device, no
                  vendor_reserved1)"
                description: |
                  For pure Matter devices without registered certificates, vendor_reserved1
                  is absent. A random 16-character hex node_id (8 bytes) is generated by the server.
                  Returns a device NOC. Call /confirm after the device installs the NOC.
                value:
                  nocsr_elements: "1530012930820122300d06092a864886f70d01010105000382010f003082010a0282010100b5c203020102301a"
                  attestation_challenge: "0102030405060708090a0b0c0d0e0f10"
                  attestation_signature: "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2"
      responses:
        "200":
          description: |
            Verification successful.
            For non-Matter groups: Node added to group.
            For Matter groups with challenge_response: Node added to group (no NOC).
            For Matter groups with nocsr_elements: Returns NOC for device to install, then call /confirm.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - $ref: "#/components/schemas/VerifyMatterResponse"
              examples:
                nonMatterChallengeResponse:
                  summary: "Non-Matter group: node added to group"
                  value:
                matterChallengeResponse:
                  summary:
                    "Matter group with challenge_response: node added to group
                    (no NOC)"
                  value:
                matterNOCSRElements:
                  summary:
                    "Matter group with nocsr_elements: returns NOC and Matter
                    Node ID"
                  value:
                    noc:
                      "-----BEGIN
                      CERTIFICATE-----\nMIIBpjCCAUygAwIBAgIIX...\n-----END
                      CERTIFICATE-----"
                    matter_node_id: "4E6F64653132330000000000"
                    node_id: "MySmartLight01"
        "400":
          description: Invalid request parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidRequestId:
                  value:
                    message: "Invalid request ID"
                invalidBody:
                  value:
                    message: "Invalid request body"
                groupIdMismatch:
                  value:
                    message: "Group ID mismatch"
                missingCSR:
                  value:
                    message: "Missing CSR for Matter group"
        "401":
          description: Invalid challenge response or user ID mismatch
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidChallengeResponse:
                  value:
                    message: "Invalid challenge response"
                userIdMismatch:
                  value:
                    message: "User ID mismatch"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                certFetchError:
                  value:
                    message: "Failed to fetch certificates for thing"
                groupAddError:
                  value:
                    message: "Failed to add node to new group"

  /v1/groups/{groupId}/node-assoc-requests/{requestId}/confirm:
    post:
      summary: Confirm node association (Matter groups only)
      description: |
        Confirms the node association after the device has successfully installed the NOC.
        This is the final step in the Matter device association flow.

        **Flow for Matter groups:**
        1. Call /initiate to get request_id and challenge
        2. Call /verify with signed challenge and CSR to get the device NOC
        3. Device installs the NOC
        4. Call /confirm to finalize the association and add node to group

        This endpoint is only used for Matter-capable groups. For non-Matter groups,
        the node is added to the group during the /verify step.
      tags:
        - Node Association
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                capabilities:
                  type: array
                  items:
                    type: string
                  description:
                    List of capabilities to enable for the node (e.g., ["acc"])
            examples:
              withCapabilities:
                summary: "Confirm with capabilities"
                description: |
                  Confirms the node association and invokes capability lambdas for each requested capability.
                value:
                  capabilities: ["acc"]
              withoutCapabilities:
                summary: "Confirm without capabilities"
                description: |
                  Confirms the node association without invoking any capability lambdas.
                value: {}
      responses:
        "200":
          description: Node successfully added to the group
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          description: Invalid request parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidRequestId:
                  value:
                    message: "Invalid request ID"
                notVerified:
                  value:
                    message: "Request not in verified status"
                groupIdMismatch:
                  value:
                    message: "Group ID mismatch"
        "401":
          description: User ID mismatch
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "User ID mismatch"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "Failed to confirm node association"

  /v1/assumed-roles:
    post:
      summary: Assume IoT user role
      description: |
        Assumes an IoT user role for the authenticated caller. The caller is identified
        via the SigV4-signed request; no AWS credentials need to be passed in the body.

        **Regular User Flow:**
        Returns credentials with IAM permissions scoped to all groups and subgroups the user has access to.

        **Admin User Flow:**
        Super admin users can use group and subgroup scoped APIs, `/v1/groups/{groupId}/assumed-roles` or `/v1/groups/{groupId}/subgroups/{subGroupId}/assumed-roles` variants instead.

        **MQTT client ID:** the returned credentials permit `iot:Connect` only with client id
        `user:<email|phone>:<session>` — the caller's own login username plus a per-session suffix.
        The bare `user:<email|phone>` form is rejected. See the ESP RainMaker Neo User MQTT API (MQTT_User) spec.
      tags:
        - Role Assumption
      security:
        - sigv4: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssumeRoleRequest"
            examples:
              Regular User:
                summary: Standard assume role for regular users
                value: {}
      responses:
        "200":
          description: Successful response with new credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssumeRoleResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/assumed-roles:
    post:
      summary: Assume IoT user role scoped to a group (super admin only)
      description: |
        Assumes an IoT user role scoped to the specified group. The returned
        credentials are restricted to this group instead of all of the caller's groups.

        Requires the user to have `custom:super_admin` set to "true" in Cognito,
        and the specified group must exist.
      tags:
        - Role Assumption
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssumeRoleRequest"
      responses:
        "200":
          description: Successful response with new credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssumeRoleResponse"
        "400":
          description: Bad request - group not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "Group not found"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description:
            Forbidden - user is not a super admin but tried to use admin
            parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "Admin privileges required"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/subgroups/{subGroupId}/assumed-roles:
    post:
      summary: Assume IoT user role scoped to a subgroup (super admin only)
      description: |
        Assumes an IoT user role scoped to the specified subgroup. The returned
        credentials are restricted to this subgroup only.

        Requires the user to have `custom:super_admin` set to "true" in Cognito,
        and the specified group and subgroup must exist.
      tags:
        - Role Assumption
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/SubGroupId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssumeRoleRequest"
      responses:
        "200":
          description: Successful response with new credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssumeRoleResponse"
        "400":
          description: Bad request - group not found or subgroup not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                groupNotFound:
                  value:
                    message: "Group not found"
                subgroupNotFound:
                  value:
                    message: "Subgroup not found"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description:
            Forbidden - user is not a super admin but tried to use admin
            parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "Admin privileges required"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/nodes/{nodeId}/assumed-roles:
    post:
      summary: Assume role for per-node S3/KVS access
      description: |
        Returns temporary credentials scoped to a single node for the requested
        services (S3 and/or KVS). The response contains ONLY the listed service
        permissions — no IoT/MQTT statements — keeping the STS session policy a
        constant size regardless of how many nodes the user can access.

        The caller is identified via the SigV4-signed request. Access is granted
        when the node belongs to the path group AND the caller has access to that
        group (full-group access, or a shared subgroup the node belongs to).
        Super admins may assume role for any node.
      tags:
        - Role Assumption
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssumeRolePerNodeRequest"
            examples:
              S3 only:
                summary: S3 credentials for one node
                value:
                  services: ["s3"]
              KVS only:
                summary: KVS viewer credentials for one node
                value:
                  services: ["kvs"]
              Both:
                summary: S3 and KVS credentials for one node
                value:
                  services: ["s3", "kvs"]
      responses:
        "200":
          description: Successful response with new credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssumeRoleResponse"
        "400":
          description:
            Bad request - missing/invalid services or unsupported service value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                servicesRequired:
                  value:
                    message: "services is required"
                unsupportedService:
                  value:
                    message: "unsupported service: mqtt"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden - caller does not have access to this node
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "User does not have access to this node"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/user/credentials:
    post:
      summary: Generate AWS credentials for the authenticated user
      description: |
        Exchanges the caller's token pair for temporary AWS credentials
        (access key id, secret access key, session token) with an expiry.

        Needs **both** tokens: `Authorization: Bearer <access_token>` plus
        `id_token` in the body, from one sign-in — an end-user pair minted by
        the ESP User OIDC issuer, or an admin pair from the admin Cognito
        pool. Both halves are verified against the issuer the access token
        names and must carry the same authentication-event id, or the request
        is rejected with `403`. The `id_token` is what the Identity Pool
        federates.

        For both users and admins.
      tags:
        - User Credentials
      security:
        - CognitoAuthorizer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id_token:
                  type: string
                  description:
                    ID token from the same sign-in as the access token in
                    the Authorization header.
              required:
                - id_token
      responses:
        "200":
          description: Successful response with AWS credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserCredsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description:
            The `id_token` does not belong to the authenticated caller, or comes
            from a different sign-in than the access token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIStatusMessage"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # ASSISTED CLAIMING
  # ----------------------------------------------------------------------

  /v1/claim/initiate:
    post:
      # Deployed only when assisted claiming is enabled. Machine-readable marker
      # for doc tooling; availability = presence of the rmng-claim-core
      # AssistedClaiming output in the published outputs.
      x-feature-flag: assisted-claiming
      summary: Reserve a node ID for a device
      description: |
        Assigns a cloud-generated node ID to a device MAC address, creating the
        reservation when none exists. The node ID is minted before any
        certificate exists, so certificate identity is server-determined.

        Idempotent per {MAC, caller}: a repeat call returns the existing node
        ID with `200` instead of `201`, and does not count against the caller's
        quota. The request `mac_addr` is never echoed back.

        Available only on deployments with assisted claiming enabled; check for
        the rmng-claim-core `AssistedClaiming` output in the published outputs
        (absent when the claim group is not deployed). When it is not deployed
        these routes do not exist, so a request is rejected at the API Gateway
        rather than reaching the service. The `404` below is the service's own
        response when claiming is inactive.
      tags:
        - Assisted Claiming
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimInitiateRequest"
      responses:
        "200":
          description: Existing reservation returned (idempotent repeat)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimInitiateResponse"
        "201":
          description: New reservation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimInitiateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Per-caller node quota reached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/claim/verify:
    post:
      # Deployed only when assisted claiming is enabled. Machine-readable marker
      # for doc tooling; availability = presence of the rmng-claim-core
      # AssistedClaiming output in the published outputs.
      x-feature-flag: assisted-claiming
      summary: Exchange a CSR for a signed certificate
      description: |
        Exchanges a device-generated CSR for a certificate signed by the
        deployment's claiming CA, and binds it to the node's IoT Thing.

        The CSR contributes only its public key; its subject is discarded and
        the certificate Common Name is rebuilt from the reservation, so the
        caller cannot influence the identity it receives. `capabilities` must
        be re-supplied on a re-claim, since they are applied to the new
        certificate; tags are never accepted from the request.

        Available only on deployments with assisted claiming enabled; check for
        the rmng-claim-core `AssistedClaiming` output in the published outputs
        (absent when the claim group is not deployed). When it is not deployed
        these routes do not exist, so a request is rejected at the API Gateway
        rather than reaching the service. The `404` below is the service's own
        response when claiming is inactive.
      tags:
        - Assisted Claiming
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimVerifyRequest"
      responses:
        "201":
          description: Certificate issued and bound to the node
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimVerifyResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: No reservation for this caller and device
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # GROUP MANAGEMENT
  # ----------------------------------------------------------------------

  /v1/groups:
    post:
      summary: Create a new group
      description: Creates a new group and associates the current user with it
      tags:
        - Group Management
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGroupRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateGroupResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    get:
      summary: List groups for the user
      description: |
        Retrieves a list of groups of current user.
      tags:
        - Group Management
      security:
        - sigv4: []
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListGroupsResponse"
              examples:
                nonMatterGroups:
                  summary: Groups without Matter capability
                  value:
                    groups:
                      - group_id: "grp_abc123"
                        group_name: "My Home"
                        access_type: "secondary"
                        node_ids:
                          - "MySmartLight01"
                          - "MySmartPlug02"
                        subgroups:
                          - subgroup_id: "sub_xyz789"
                            subgroup_name: "Living Room"
                            node_ids:
                              - "MySmartLight01"
                matterEnabledGroups:
                  summary: Groups with Matter capability
                  value:
                    groups:
                      - group_id: "grp_abc123"
                        group_name: "My Home"
                        access_type: "primary"
                        node_ids:
                          - "MySmartLight01"
                          - "MySmartPlug02"
                        subgroups:
                          - subgroup_id: "sub_xyz789"
                            subgroup_name: "Living Room"
                            node_ids:
                              - "MySmartLight01"
                      - group_id: "grp_matter456"
                        group_name: "Matter Home"
                        access_type: "primary"
                        node_ids:
                          - "MatterDevice01"
                        subgroups: []
                        matter:
                          fabric_id: "6162633132330000"
                          root_ca:
                            "-----BEGIN
                            CERTIFICATE-----\nMIIBpjCCAUygAwIBAgIIX...\n-----END
                            CERTIFICATE-----"
                          ipk: "0123456789ABCDEF0123456789ABCDEF"
                          group_cat_id_admin: "01000001"
                          group_cat_id_operate: "06000001"
                groupsWithCapabilities:
                  summary: Groups with node capabilities (e.g., ACC)
                  value:
                    groups:
                      - group_id: "grp_abc123"
                        group_name: "Living Room"
                        access_type: "primary"
                        node_ids:
                          - "node1"
                          - "node2"
                        node_details:
                          node1:
                            capabilities: ["rmng", "matter", "acc"]
                            capability_details:
                              acc:
                                endpoint_id: "de152ff2-f070-4d0e-94da-770828b1770f"
                              matter:
                                matter_node_id: "B2C3D4E5F6071829"
                          node2:
                            capabilities: ["matter"]
                            capability_details:
                              matter:
                                matter_node_id: "B2C3D4E5F6071829"
                        subgroups:
                          - subgroup_id: "sub1"
                            subgroup_name: "Corner Lights"
                            node_ids:
                              - "node1"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/subgroups:
    post:
      summary: Create a new subgroup
      description: Creates a new subgroup within the specified group
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSubgroupRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSubgroupResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}:
    patch:
      summary: Update group information
      description: Updates the information of an existing group
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateGroupRequest"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

    delete:
      summary: Delete a group
      description: |
        Deletes an empty group. The group must have no nodes and no
        subgroups; otherwise the request is rejected with 409. Remove all
        nodes and delete all subgroups first, then delete the group.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      responses:
        "200":
          description: Successful deletion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIStatusMessage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: Group is not empty (still has nodes or subgroups)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIStatusMessage"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/subgroups/{subGroupId}:
    patch:
      summary: Update subgroup information
      description: Updates the information of an existing subgroup
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/SubGroupId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSubgroupRequest"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

    delete:
      summary: Delete a subgroup
      description: |
        Deletes an empty subgroup. The subgroup must have no nodes;
        otherwise the request is rejected with 409. Remove all nodes from
        the subgroup first, then delete it.
        - Removes the subgroup from the database.
        - Removes associated user-subgroup mappings.
        - Does NOT delete the nodes.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - in: path
          name: groupId
          required: true
          schema:
            type: string
          description: The ID of the group containing the subgroup
        - in: path
          name: subGroupId
          required: true
          schema:
            type: string
          description: The ID of the subgroup to delete
      responses:
        "200":
          description: Successful deletion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Subgroup not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Subgroup is not empty (still has nodes)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/nodes/{nodeId}:
    delete:
      summary: Remove a node from a group (disassociation)
      description:
        Removes a node from the specified group. Requires the caller to be the
        sole primary user of the group. Not allowed for Matter-capable nodes.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Node successfully removed from group
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupEditResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/subgroups/{subGroupId}/nodes/{nodeId}:
    put:
      summary: Add a node to a subgroup
      description: Adds a node to a specified subgroup
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/SubGroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupEditResponse"

    delete:
      summary: Remove a node from a subgroup
      description: |
        Removes a node from a specific subgroup while keeping it in the main group.
        The node must already be in the subgroup for this operation to succeed.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/SubGroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Node successfully removed from subgroup
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupEditResponse"
        "500":
          description: Failed to remove node from subgroup
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  # ----------------------------------------------------------------------
  # LIST GROUP USERS
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/users:
    get:
      summary: List users in a group
      description: |
        Returns users who have access to the specified group, along with
        their email, phone number, and access type. The listing scope depends
        on the caller's access level:
        - **Primary Access**: Sees all users (primary, secondary, and subentity users)
        - **Secondary Access**: Sees only primary users (for discovering group owners)
        - **Subentity Access**: Denied (requires group-level access)

        Useful for discovering user IDs before issuing unshare (DELETE) requests.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListGroupUsersResponse"
              examples:
                primaryCallerSeesAll:
                  summary:
                    Primary caller sees all users (primary, secondary,
                    subentity)
                  value:
                    users:
                      - user_id: "user-abc-123"
                        email: "owner@example.com"
                        access_type: "primary"
                      - user_id: "user-def-456"
                        email: "guest@example.com"
                        phone_number: "+1234567890"
                        access_type: "secondary"
                      - user_id: "user-ghi-789"
                        email: "subgroup-user@example.com"
                        access_type: "subgroup"
                        subgroups:
                          - "subgrp-001"
                secondaryCallerSeesOnlyPrimary:
                  summary: Secondary caller sees only primary users
                  value:
                    users:
                      - user_id: "user-abc-123"
                        email: "owner@example.com"
                        access_type: "primary"
        "400":
          description: Missing group ID or caller has no group-level access
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/subgroups/{subGroupId}/users:
    get:
      summary: List users in a subgroup
      description: |
        Returns the users who have access to the specified subgroup, along
        with their email, phone number, and access type. Every returned
        entry's `subgroups` is scoped to just this subgroup, regardless of
        whether the user's actual access is group-wide or subgroup-specific.

        The listing scope is determined by the caller's access level:
        - Primary callers see all users (primary, secondary, and subgroup users with access to this subgroup).
        - Secondary and subgroup-only callers see only the primary owners.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/SubGroupId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListGroupUsersResponse"
              examples:
                fullListing:
                  summary: Primary caller sees all users
                  value:
                    users:
                      - user_id: "user-abc-123"
                        email: "owner@example.com"
                        access_type: "primary"
                      - user_id: "user-ghi-789"
                        email: "subgroup-user@example.com"
                        access_type: "subgroup"
                        subgroups:
                          - "subgrp-001"
                primaryOwnersOnly:
                  summary: Subgroup-only caller sees only primary owners
                  value:
                    users:
                      - user_id: "user-abc-123"
                        email: "owner@example.com"
                        access_type: "primary"
        "400":
          description: Missing group ID or missing subgroup ID
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # REMOVE MEMBER (Kick user or User leaving)
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/users/{userId}:
    delete:
      summary: Remove member from group
      description: |
        Removes an active member from the group.
        - If called by an **Admin**: Acts as "Kicking" the user.
        - If called by the **User themselves** (via 'me' alias): Acts as "Leaving" the group.
        - A Primary user can remove a Secondary user from the group. He can also remove other Primary users from the group.
        - The last remaining Primary user cannot be removed; the group must always have at least one Primary. Use remove node from group instead.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - name: userId
          in: path
          required: true
          schema:
            type: string
          description: The ID of the user to remove (or 'me').
      responses:
        "200":
          description: Member successfully removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "404":
          description: Member not found in this group
        "403":
          description: You do not have permission to remove this member
        "409":
          description: Cannot remove the last primary user from the group

  /v1/groups/{groupId}/subgroups/{subGroupId}/users/{userId}:
    delete:
      summary: Remove member from subgroup
      description: |
        Removes an active member from the subgroup.
        - If called by an **Admin**: Acts as "Kicking" the user.
        - If called by the **User themselves** (via 'me' alias): Acts as "Leaving" the group.
      tags:
        - Group Management
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/SubGroupId"
        - name: userId
          in: path
          required: true
          schema:
            type: string
          description: The ID of the user to remove (or 'me').
      responses:
        "200":
          description: Member successfully removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "404":
          description: Member not found in this group
        "403":
          description: You do not have permission to remove this member

  /v1/groups/{groupId}/matter-nocs:
    post:
      summary: Generate Matter NOC for user
      description: |
        Generates a Node Operational Certificate (NOC) for the current user in a Matter-enabled group.
        Each phone app installation must retain its own ECDSA P-256 operational key and provide a
        Certificate Signing Request (CSR) for that key. The NOC is signed by the group's Root
        CA and uses a user Matter Node ID derived from the authenticated user, fabric, and key.
      tags:
        - Matter Fabric
      security:
        - sigv4: []
      parameters:
        - in: path
          name: groupId
          required: true
          schema:
            type: string
          description: The ID of the group (must have Matter capability)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MatterNOCRequest"
      responses:
        "200":
          description: NOC generated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatterNOCResponse"
        "400":
          description:
            Invalid request (missing CSR or group doesn't have Matter
            capability)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Group not found or not accessible
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/capabilities:
    post:
      summary: Enable capabilities on an existing group
      description: |
        Enables the capabilities listed in the request body on an existing group.
        Only the group's primary owner may change capabilities. Each requested
        capability must be known and not already enabled on the group.

        Enabling the "matter" capability turns the group into a Matter fabric,
        generating the fabric's Root CA, IPK and CAT IDs (returned in the `matter`
        field).
      tags:
        - Matter Fabric
      security:
        - sigv4: []
      parameters:
        - in: path
          name: groupId
          required: true
          schema:
            type: string
          description: The ID of the group to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddGroupCapabilitiesRequest"
      responses:
        "200":
          description: Capabilities enabled successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddGroupCapabilitiesResponse"
        "400":
          description: Missing or unknown capabilities, or group not found / not accessible / not owned by the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: A requested capability is already enabled on the group
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/nodes/{nodeId}/config:
    get:
      summary: Get node configuration
      description: |
        Retrieves the complete configuration data for a node.
        This endpoint returns the entire config object stored in the node_config table.
        For RainMaker devices, config is written only by the device firmware. PUT is
        allowed only for pure Matter nodes (see below); DELETE is not supported.
      tags:
        - Node Configuration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeConfig"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          description: |
            Returned when the node has no config stored, or for PUT/DELETE
            (see below).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      summary: Update node configuration (pure Matter nodes only)
      description: |
        Writes the configuration for a pure Matter node. Pure Matter nodes have no
        RainMaker firmware to publish their own config, so the app is the only source
        of it and supplies a complete Matter `NodeConfig` (`data_model: "matter"` with
        `endpoints`), which is stored verbatim.

        This is permitted only for pure Matter nodes - those tagged with the `matter`
        capability and not the `rmng` capability. It is rejected for RainMaker devices
        (including Matter-fabric-joined RainMaker devices), whose config is written only
        by the device firmware.
      tags:
        - Node Configuration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NodeConfig"
            example:
              data_model: "matter"
              info:
                name: "Living Room Light"
                type: "matter"
                fw_version: "1.0"
                model: "0x010D"
              endpoints:
                "0x0":
                  dt: "0x0016"
                  c:
                    s:
                      "0x1d": {}
                      "0x28": {}
                      "0x1f": {}
                      "0x3e": {}
                "0x1":
                  dt: "0x010D"
                  c:
                    s:
                      "0x3": {}
                      "0x4": {}
                      "0x6":
                        a: ["0x0"]
                      "0x8":
                        a: ["0x0"]
                      "0x300":
                        a: ["0x7", "0x8", "0x0f"]
                      "0x1d": {}
      responses:
        "200":
          description: Config stored successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |
            The node is not a pure Matter node, or the caller lacks access to it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "config write is only allowed for pure Matter nodes"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      summary: Delete node configuration (Not Allowed)
      description: |
        This operation is not allowed for the config service.
        Node configurations can only be deleted by the device itself.
      tags:
        - Node Configuration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "500":
          description: Operation not permitted for this service
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                message: "DELETE operation not allowed for config service"

  /v1/groups/{groupId}/sharing-requests:
    post:
      summary: Send group sharing-requests
      description: |
        Creates a new sharing request for a user to join the group. The invitee
        is named by `username` — the email address or E.164 phone number they
        sign in with. The user will see this in their 'sharing-requests' inbox.
      tags:
        - Group Sharing
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSharingRequestRequest"
      responses:
        "201":
          description: Invitation sent successfully
          content:
            application/json:
              schema:
                type: object
                required: [request_id, message]
                properties:
                  request_id:
                    type: string
                    format: uuid
                  message:
                    type: string
              example:
                request_id: 1c9b2f4e-6a3d-4c8b-9e2f-7d5a1b3c4d5e
                message: Invite sent if the user is registered with ESP RainMaker Neo
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/subgroups/{subGroupId}/sharing-requests:
    post:
      summary: Send subgroup sharing-requests
      description: |
        Creates a new sharing request for a user to join the subgroup. The
        invitee is named by `username` — the email address or E.164 phone
        number they sign in with.
      tags:
        - Subgroup Sharing
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/SubGroupId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSharingRequestRequest"
      responses:
        "201":
          description: |
            Request accepted. The response is identical whether or not the
            username belongs to a registered user: same message, and a
            request_id that is real for a registered invitee and a decoy UUID
            otherwise, so the outcome does not reveal whether an account exists.
          content:
            application/json:
              schema:
                type: object
                required: [request_id, message]
                properties:
                  request_id:
                    type: string
                    format: uuid
                  message:
                    type: string
              example:
                request_id: 1c9b2f4e-6a3d-4c8b-9e2f-7d5a1b3c4d5e
                message: Invite sent if the user is registered with ESP RainMaker Neo
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/sharing-requests/received:
    get:
      summary: List received sharing requests
      description:
        Retrieves a list of requests where groups are asking the user to join
        (Pending Invitations). For the current user, use the 'me' alias.
      tags:
        - Sharing Requests
      security:
        - sigv4: []
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSharingRequestsResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # 1. ACCEPT Action
  /v1/sharing-requests/{requestId}/accept:
    post:
      summary: Accept sharing request
      description: >
        Accepts the invitation. This triggers the 'join' workflow, adds the user
        to the group, and may send notifications.
      tags:
        - Sharing Requests
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/RequestId"
      # No complex body needed usually, unless you have metadata
      responses:
        "200":
          description: Request accepted successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIStatusMessage"
              example:
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # 2. REJECT Action
  /v1/sharing-requests/{requestId}/reject:
    post:
      summary: Reject sharing request
      description: Declines the invitation and marks the request as rejected.
      tags:
        - Sharing Requests
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Request rejected successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIStatusMessage"
              example:
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # INTEGRATIONS (non-admin) — apps call this to discover the integration_id
  # to register a delivery endpoint against. Unlike the admin list, this
  # returns only the public id+type per integration; no credentials.
  # ----------------------------------------------------------------------
  /v1/integrations:
    get:
      summary: List integrations (non-admin)
      description: |
        Returns the `integration_id` / `integration_type` of every configured
        integration. Any authenticated user may call this — it is the
        non-admin counterpart of `GET /v1/admin/integrations` and exposes no
        credentials or other configuration.

        Apps use the returned `integration_id` when registering a delivery
        endpoint via `PUT /v1/integrations/{integrationId}/endpoints`.
      tags:
        - Notification Endpoints
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/IntegrationTypeQueryOptional"
      responses:
        "200":
          description: Successfully retrieved integrations list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPublicIntegrationsResponse"
              example:
                integrations:
                  - integration_id: "apns_com.company.app"
                    integration_type: "apns"
                  - integration_id: "apns_sandbox_com.company.app"
                    integration_type: "apns_sandbox"
                  - integration_id: "gcm_your-fcm-project-id"
                    integration_type: "gcm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # ENDPOINTS — user-facing registration of delivery endpoints. A single user
  # can have multiple endpoints per integration (e.g. multiple GCM devices,
  # multiple linked Amazon accounts). PUT registers a new endpoint and returns
  # its server-derived endpoint_id; DELETE addresses one specific endpoint.
  # ----------------------------------------------------------------------
  # NOTE (not rendered): For Alexa and GVA integrations, endpoint
  # registration happens internally as part of the account-link OAuth flow
  # — public callers do not PUT to this endpoint for those types. Webhook
  # integrations have no public surface today; the `webhook_mock` platform
  # exercises this path in itest only (test/itest/test_notifications.py).
  /v1/integrations/{integrationId}/endpoints:
    parameters:
      - $ref: "#/components/parameters/IntegrationId"
    put:
      summary: Register a delivery endpoint on this integration
      description: |
        Registers a delivery endpoint for the calling user on the given
        integration. The server derives an `endpoint_id` per integration
        type and returns it in the response.
        Callers must persist the returned `endpoint_id` to later address
        the endpoint (e.g. for DELETE).
      tags:
        - Notification Endpoints
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterEndpointRequest"
            examples:
              APNS:
                summary: APNS push registration (iOS)
                value:
                  delivery_credentials:
                    app_token: "<apns-device-token>"
                  locale: "en_US"
              GCM:
                summary: GCM push registration (Android)
                value:
                  delivery_credentials:
                    app_token: "<fcm-registration-token>"
                  locale: "en_US"
      responses:
        "200":
          description: Endpoint registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterEndpointResponse"
              example:
                status: "success"
                endpoint_id: "arn:aws:sns:us-east-1:123456789012:endpoint/APNS/RainMaker/abc-123"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/integrations/{integrationId}/endpoints/{endpointId}:
    parameters:
      - $ref: "#/components/parameters/IntegrationId"
      - in: path
        name: endpointId
        required: true
        schema:
          type: string
        description: |
          The per-endpoint identifier returned by `PUT /v1/integrations/{integrationId}/endpoints`.
    delete:
      summary: Unregister one specific endpoint
      description: |
        Removes the named endpoint for the calling user on the given
        integration and cleans up the underlying delivery registration
      tags:
        - Notification Endpoints
      security:
        - sigv4: []
      responses:
        "200":
          description: Endpoint successfully unregistered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # TIMESERIES: RAW DATA (Time-series points)
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/nodes/{nodeId}/timeseries/raw:
    get:
      summary: Retrieve raw timeseries data
      description: |
        Retrieves raw data points for a specific parameter within a time range.
      tags:
        - Node Timeseries
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
        - in: query
          name: key
          required: true
          schema:
            type: string
          description: |
            Data point key — within-node identifier. Default data model:
            `<device id>.<param id>` (e.g., `Sensor.Temperature`).
            Matter data model: the full dotted key chain
            `0x<endpoint>.c.s.0x<cluster>.a.0x<attribute>`
            (e.g., `0x1.c.s.0x402.a.0x0`).
        - in: query
          name: data_type
          required: true
          schema:
            type: string
          description: Data type (e.g., float, int).
        - in: query
          name: start_time
          required: true
          schema:
            type: integer
            format: int64
          description: Start timestamp in milliseconds.
        - in: query
          name: end_time
          required: false
          schema:
            type: integer
            format: int64
          description: End timestamp in milliseconds (defaults to now).
        - $ref: "#/components/parameters/PageSize"
        - $ref: "#/components/parameters/StartKey"
      responses:
        "200":
          description: |
            List of raw data points. If the request is made with no query
            parameters at all, a self-describing usage document
            (`TimeseriesServiceInfoResponse`) is returned instead.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/TimeseriesRawDataResponse"
                  - $ref: "#/components/schemas/TimeseriesServiceInfoResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # TIMESERIES: LATEST DATA (Snapshot)
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/nodes/{nodeId}/timeseries/latest:
    get:
      summary: Retrieve latest data point
      description: Retrieves the single most recent data point for a parameter.
      tags:
        - Node Timeseries
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
        - in: query
          name: key
          required: true
          schema:
            type: string
          description: Data point key.
        - in: query
          name: data_type
          required: true
          schema:
            type: string
          description: Data type (e.g., float, int).
      responses:
        "200":
          description: |
            The latest data point. If the request is made with no query
            parameters at all, a self-describing usage document
            (`TimeseriesServiceInfoResponse`) is returned instead.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/TimeseriesLatestDataResponse"
                  - $ref: "#/components/schemas/TimeseriesServiceInfoResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # TIMESERIES: AGGREGATES (Stats)
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/nodes/{nodeId}/timeseries/aggregates:
    get:
      summary: Retrieve aggregated data
      description: |
        Retrieves statistical aggregates (min, max, avg, sum) for a parameter.
        Supports query by specific date or date range. Range queries are
        paginated.
      tags:
        - Node Timeseries
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
        - in: query
          name: key
          required: true
          schema:
            type: string
          description: Data point key.
        - in: query
          name: data_type
          required: true
          schema:
            type: string
          description: Data type (e.g., float, int).
        - in: query
          name: window
          required: true
          schema:
            type: string
            enum: [hourly, daily, weekly, monthly]
          description: The aggregation window/bucket size.

        # Date Filters (Grouped logically)
        - in: query
          name: date
          schema:
            type: string
            pattern: '^\d{4}-\d{2}-\d{2}(T\d{2})?$'
          description: Specific date/hour to fetch (historical).
        - in: query
          name: start_date
          schema:
            type: string
          description: Range start date.
        - in: query
          name: end_date
          schema:
            type: string
          description: Range end date.
        - $ref: "#/components/parameters/PageSize"
        - $ref: "#/components/parameters/StartKey"

      responses:
        "200":
          description: |
            Aggregated statistics. Without `date`/`start_date`/`end_date` the
            live rollup for the requested `window` is returned; with `date` a
            single historical window is returned; with `start_date`/`end_date`
            a paginated historical range is returned. If the request is made
            with no query parameters at all, a self-describing usage document
            (`TimeseriesServiceInfoResponse`) is returned instead.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/TimeseriesAggregatesResponse"
                  - $ref: "#/components/schemas/TimeseriesServiceInfoResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/nodes/{nodeId}/schedules:
    get:
      summary: Get node schedules
      description: |
        Retrieves the schedule configuration for a node.
        Returns an empty object if no schedule is configured.
      tags:
        - Node Schedule
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeSchedulesPayload"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    put:
      summary: Create/Update node schedules
      description: |
        Creates or replaces the schedule configuration for a node.
        The schedule data should contain an array of schedules, each with triggers and actions.
        The device will be notified of the schedule update.
      tags:
        - Node Schedule
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NodeSchedulesPayload"
      responses:
        "200":
          description: Schedules updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    delete:
      summary: Delete node schedules
      description: |
        Deletes the schedule configuration for a node.
        The device will be notified of the schedule deletion.
      tags:
        - Node Schedule
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Schedules deleted successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes:
    post:
      summary: Register a single node
      description: |
        Synchronously registers a single node using a provided certificate string.
        The certificate is used to create a thing in AWS IoT Core
        Returns the created node ID immediately.
      tags:
        - Node Administration
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterSingleNodeRequest"
      responses:
        "201":
          description: Node successfully registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterSingleNodeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes/registration-jobs:
    get:
      summary: List bulk node registration jobs
      description: |
        Lists all bulk node registration jobs, ordered by creation time
        (newest first). Paginated: server-side default page size is 20.
      tags:
        - Node Administration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/PageSize"
        - $ref: "#/components/parameters/StartKey"
        - in: query
          name: status
          schema:
            type: string
            enum: [requested, started, data_loaded, completed]
          description: Filter jobs by status
      responses:
        "200":
          description: List of registration jobs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRegistrationJobsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      summary: Bulk register nodes via S3
      description: |
        Initiates an asynchronous job to register multiple nodes from a CSV file stored in S3.
        Returns a job ID to track the registration progress.
      tags:
        - Node Administration
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterBulkNodesRequest"
      responses:
        "202": # 202 Accepted indicates the request is valid and queued for processing
          description: Bulk registration job accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterBulkNodesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes/registration-jobs/{requestId}:
    get:
      summary: Get bulk node registration status
      description: |
        Retrieves the status of a bulk node registration request by request ID.
        Returns the current status, counts, and metadata for the request.
      tags:
        - Node Administration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Status retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterNodeStatusResponse"
        "404":
          description: |
            Registration job not found. Returned when the request_id
            does not exist OR when it belongs to an update job
            (cross-flow scoping: an update job_id queried on this path
            looks like a 404 rather than leaking the update row).
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes/registration-jobs/{requestId}/failed-nodes:
    get:
      summary: List per-node failures for a registration job
      description: |
        Paginated list of per-node failure detail (node_id, code, reason,
        recorded_at) for the given registration job. Use this when the
        job's aggregate `failed_count` is non-zero and you need to know
        which nodes failed and why.

        Returns 200 with `failed_nodes: []` when the job exists but has
        zero failures; returns 404 when the job doesn't exist or belongs
        to a different flow.

        Failures are written in a single batch at end-of-job, so polling
        this endpoint mid-run returns an empty list. The accompanying
        job status's `status=completed` indicates the failure detail is
        final.
      tags:
        - Node Administration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/PageSize"
        - $ref: "#/components/parameters/StartKey"
      responses:
        "200":
          description: Page of failed-node rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFailedNodesResponse"
        "404":
          description: Registration job not found
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes/update-jobs:
    post:
      summary: Bulk update nodes via S3
      description: |
        Initiates an asynchronous job to update already-registered nodes
        from a CSV file stored in S3. Use this for two related operations
        on existing nodes:

        - Metadata updates (admin groups, tags, plus any extra CSV
          columns treated as `key:value` tags via the registration
          convention).
        - Cert update: when a CSV row has a non-empty `certs` column the
          node's cloud-side cert is replaced (register new, attach,
          deactivate the old). This is operator correction for a
          mistakenly-registered PEM — it does not push the new cert to
          the device.

        Symmetric with `POST .../registration-jobs`: returns
        `202 Accepted` with a `request_id`, kicks off the shared Fargate
        task, status visible via `GET .../update-jobs/{requestId}` and
        `GET .../update-jobs/{requestId}/failed-nodes`.
      tags:
        - Node Administration
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkUpdateNodesRequest"
      responses:
        "202":
          description: Bulk update job accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkUpdateNodesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes/update-jobs/{requestId}:
    get:
      summary: Get bulk node update status
      description: |
        Retrieves the status of a bulk node update request by request ID.
        Same response shape as the registration-jobs status endpoint —
        the `job_type` field discriminates.
      tags:
        - Node Administration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Status retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterNodeStatusResponse"
        "404":
          description: |
            Update job not found. Returned when the request_id does not
            exist OR when it belongs to a registration job (cross-flow
            scoping mirror of the registration-jobs endpoint).
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes/update-jobs/{requestId}/failed-nodes:
    get:
      summary: List per-node failures for an update job
      description: |
        Paginated list of per-node failure detail for the given update
        job. Same shape and semantics as the registration-jobs
        equivalent.
      tags:
        - Node Administration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/PageSize"
        - $ref: "#/components/parameters/StartKey"
      responses:
        "200":
          description: Page of failed-node rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFailedNodesResponse"
        "404":
          description: Update job not found
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/nodes/{nodeId}/groups:
    get:
      summary: Get node's group and subgroups
      description: |
        Returns the group and subgroups that a node belongs to.
        If the node is not in any group, returns an empty group and empty sub_groups array.
        Only super admin users can call this endpoint.
      tags:
        - Node Administration
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Node group info retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  group:
                    type: string
                    description:
                      The group ID the node belongs to (empty string if none)
                  sub_groups:
                    type: array
                    items:
                      type: string
                    description: List of subgroup IDs the node belongs to
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # ADMIN — unified integration tree. All integration types (push,
  # smart-home, webhook) share the same paths; `integration_type` is a
  # query parameter on POST/list and a property of the integration record
  # on read.
  # ----------------------------------------------------------------------
  # NOTE (not rendered): Alexa and GVA integrations are registered
  # internally during deployment setup and are not normally created via
  # this public API. Webhook integrations are not yet fully supported.
  /v1/admin/integrations:
    post:
      summary: Register an integration
      description: |
        Registers a new integration. The `integration_type` query parameter
        selects the body shape (see `IntegrationConfigRequest`). Returns
        the assigned `integration_id` which subsequent operations (GET /
        PUT / DELETE / endpoint registration) address by.

        Super-admin only.
      tags:
        - Integrations
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/IntegrationTypeQuery"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntegrationConfigRequest"
            examples:
              APNS:
                summary: APNS (iOS production)
                value:
                  authentication_key: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
                  key_id: "<10 char Apple Key ID>"
                  team_id: "<10 char Apple Team ID>"
                  bundle_id: "com.company.app"
              APNS_SANDBOX:
                summary: APNS (iOS sandbox)
                value:
                  authentication_key: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
                  key_id: "<10 char Apple Key ID>"
                  team_id: "<10 char Apple Team ID>"
                  bundle_id: "com.company.app"
              GCM:
                summary:
                  GCM (Android) — Google service-account JSON, same shape as GVA
                value:
                  type: "service_account"
                  project_id: "your-fcm-project-id"
                  private_key_id: "abc123def456"
                  private_key: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
                  client_email: "firebase-adminsdk-xyzyour-fcm-project-id.iam.gserviceaccount.com"
                  client_id: "104567890123456789012"
                  auth_uri: "https://accounts.google.com/o/oauth2/auth"
                  token_uri: "https://oauth2.googleapis.com/token"
                  auth_provider_x509_cert_url: "https://www.googleapis.com/oauth2/v1/certs"
                  client_x509_cert_url: "https://www.googleapis.com/robot/v1/metadata/x509/firebase-adminsdk-xyz%40your-fcm-project-id.iam.gserviceaccount.com"
                  universe_domain: "googleapis.com"
              Alexa:
                summary: Alexa skill
                value:
                  client_id: "amzn1.application-oa2-client.<client-id>"
                  client_secret: "amzn1.oa2-cs.v1.<client-secret>"
                  skill_id: "amzn1.ask.skill.<skill-id>"
                  manufacturer_name: "Espressif"
                  redirect_uris:
                    - "https://pitangui.amazon.com/api/skill/link/<skill-link-id>"
                    - "https://layla.amazon.com/api/skill/link/<skill-link-id>"
                    - "https://alexa.amazon.co.jp/api/skill/link/<skill-link-id>"
              GVA:
                summary: Google Voice Assistant
                value:
                  type: "service_account"
                  project_id: "your-gva-project-id"
                  private_key_id: "abc123def456789"
                  private_key: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
                  client_email: "homegraph-sa@your-gva-project-id.iam.gserviceaccount.com"
                  client_id: "104567890123456789012"
                  auth_uri: "https://accounts.google.com/o/oauth2/auth"
                  token_uri: "https://oauth2.googleapis.com/token"
                  auth_provider_x509_cert_url: "https://www.googleapis.com/oauth2/v1/certs"
                  client_x509_cert_url: "https://www.googleapis.com/robot/v1/metadata/x509/homegraph-sa%40your-gva-project-id.iam.gserviceaccount.com"
                  universe_domain: "googleapis.com"
      responses:
        "201":
          description: Integration successfully registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterIntegrationResponse"
              example:
                integration_id: "apns_com.company.app"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    get:
      summary: List integrations
      description: |
        Returns all configured integrations, optionally filtered by
        `integration_type`. Super-admin only.
      tags:
        - Integrations
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/IntegrationTypeQueryOptional"
      responses:
        "200":
          description: Successfully retrieved integrations list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListIntegrationsResponse"
              example:
                integrations:
                  - integration_id: "apns_com.company.app"
                    integration_type: "apns"
                  - integration_id: "apns_sandbox_com.company.app"
                    integration_type: "apns_sandbox"
                  - integration_id: "gcm_your-fcm-project-id"
                    integration_type: "gcm"
                  - integration_id: "alexa_default"
                    integration_type: "alexa"
                  - integration_id: "gva_your-gva-project-id"
                    integration_type: "gva"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/integrations/{integrationId}:
    parameters:
      - $ref: "#/components/parameters/IntegrationId"
    get:
      summary: Get an integration
      description: |
        Retrieves the configuration for one integration. Client secrets
        are never returned. Super-admin only.
      tags:
        - Integrations
      security:
        - sigv4: []
      responses:
        "200":
          description: Integration retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntegrationConfigResponse"
              examples:
                APNS:
                  value:
                    integration_id: "apns_com.company.app"
                    integration_type: "apns"
                    key_id: "<10 char Apple Key ID>"
                    team_id: "<10 char Apple Team ID>"
                    bundle_id: "com.company.app"
                GCM:
                  value:
                    integration_id: "gcm_your-fcm-project-id"
                    integration_type: "gcm"
                    project_id: "your-fcm-project-id"
                Alexa:
                  value:
                    integration_id: "alexa_default"
                    integration_type: "alexa"
                    client_id: "amzn1.application-oa2-client.<client-id>"
                    skill_id: "amzn1.ask.skill.<skill-id>"
                    manufacturer_name: "Espressif"
                    redirect_uris:
                      - "https://pitangui.amazon.com/api/skill/link/<skill-link-id>"
                      - "https://layla.amazon.com/api/skill/link/<skill-link-id>"
                      - "https://alexa.amazon.co.jp/api/skill/link/<skill-link-id>"
                GVA:
                  value:
                    integration_id: "gva_your-gva-project-id"
                    integration_type: "gva"
                    type: "service_account"
                    project_id: "your-gva-project-id"
                    private_key_id: "abc123def456789"
                    client_email: "homegraph-sa@your-gva-prod.iam.gserviceaccount.com"
                    client_id: "104567890123456789012"
                    auth_uri: "https://accounts.google.com/o/oauth2/auth"
                    token_uri: "https://oauth2.googleapis.com/token"
                    auth_provider_x509_cert_url: "https://www.googleapis.com/oauth2/v1/certs"
                    client_x509_cert_url: "https://www.googleapis.com/robot/v1/metadata/x509/homegraph-sa%your-gva-prod.iam.gserviceaccount.com"
                    universe_domain: "googleapis.com"
                    redirect_uris:
                      - "https://oauth-redirect.googleusercontent.com/r/your-gva-prod"
        "404":
          description: Integration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    put:
      summary: Update integration credentials
      description: |
        Rotates the credentials for an existing integration. Existing
        endpoints are unaffected. The request body must match the
        integration's type. Super-admin only.
      tags:
        - Integrations
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntegrationConfigRequest"
            examples:
              APNS:
                summary: Rotate APNS auth key
                description: |
                  Same shape as POST. Typical rotation: new `key_id` paired
                  with the corresponding new `.p8` material; `team_id` and
                  `bundle_id` stay the same.
                value:
                  authentication_key: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
                  key_id: "<10 char Apple Key ID>"
                  team_id: "<10 char Apple Team ID>"
                  bundle_id: "com.company.app"
              GCM:
                summary: Rotate GCM service account
                description: |
                  Provide a new service-account JSON. `project_id` must
                  match the existing integration — otherwise the request
                  is rejected. Same shape as the POST body.
                value:
                  type: "service_account"
                  project_id: "your-fcm-project-id"
                  private_key_id: "rotated_xyz789"
                  private_key: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
                  client_email: "firebase-adminsdk-xyz@your-prod-12345.iam.gserviceaccount.com"
                  client_id: "104567890123456789012"
                  auth_uri: "https://accounts.google.com/o/oauth2/auth"
                  token_uri: "https://oauth2.googleapis.com/token"
                  auth_provider_x509_cert_url: "https://www.googleapis.com/oauth2/v1/certs"
                  client_x509_cert_url: "https://www.googleapis.com/robot/v1/metadata/x509/firebase-adminsdk-xyz%40your-fcm-project-id.iam.gserviceaccount.com"
                  universe_domain: "googleapis.com"
              Alexa:
                summary: Rotate Alexa client secret
                value:
                  client_id: "amzn1.application-oa2-client.6b3c8e2a4f5d4b9c8a7e2f1d3b5a8c6e"
                  client_secret: "amzn1.oa2-cs.v1.<client-secret>"
                  skill_id: "amzn1.ask.skill.f5e4d3c2-b1a0-9876-5432-1fedcba98765"
                  redirect_uris:
                    - "https://pitangui.amazon.com/api/skill/link/<skill-link-id>"
                    - "https://layla.amazon.com/api/skill/link/<skill-link-id>"
                    - "https://alexa.amazon.co.jp/api/skill/link/<skill-link-id>"
              GVA:
                summary: Rotate GVA service-account key
                description: |
                  Provide a new HomeGraph service-account JSON. `project_id`
                  must match the existing integration; rotating typically
                  means a new `private_key_id` + `private_key` for the same
                  `client_email`.
                value:
                  type: "service_account"
                  project_id: "your-gva-project-id"
                  private_key_id: "rotated_xyz987"
                  private_key: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
                  client_email: "homegraph-sa@your-gva-prod.iam.gserviceaccount.com"
                  client_id: "104567890123456789012"
                  auth_uri: "https://accounts.google.com/o/oauth2/auth"
                  token_uri: "https://oauth2.googleapis.com/token"
                  auth_provider_x509_cert_url: "https://www.googleapis.com/oauth2/v1/certs"
                  client_x509_cert_url: "https://www.googleapis.com/robot/v1/metadata/x509/homegraph-sa%40your-gva-project-id.iam.gserviceaccount.com"
                  universe_domain: "googleapis.com"
      responses:
        "200":
          description: Credentials updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    delete:
      summary: Delete an integration
      description: |
        Deletes an integration. Existing endpoints registered against it
        will start failing on the next delivery. Super-admin only.
      tags:
        - Integrations
      security:
        - sigv4: []
      responses:
        "200":
          description: Integration deleted successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/iot-event-mode:
    get:
      summary: Get the current IoT-rule action mode for event-handler lambdas
      description: |
        Returns the action currently attached to `node_disconnected_rule` and
        `node_to_cloud_rule` — either `direct` (IoT rule invokes the lambda
        directly) or `sqs` (IoT rule writes to an SQS queue that the lambda
        consumes in batches).
        Both modes are pre-provisioned at deploy time; this endpoint reports
        which one is currently active. Only super admin users can access this
        endpoint.
      tags:
        - Node Administration
      security:
        - sigv4: []
      responses:
        "200":
          description: Current IoT-rule action mode for both rules
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IotEventModeResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    put:
      summary: Flip the IoT-rule action mode for event-handler lambdas
      description: |
        Replaces the action on `node_disconnected_rule` and `node_to_cloud_rule`
        with either the Lambda-direct action or the SQS action. Both rules
        flip together in one call.
        The change takes effect immediately and does not require a redeploy.
        SQL, SQL version, description, disabled flag, and error_action on
        each rule are preserved across the flip.
        Only super admin users can access this endpoint.
      tags:
        - Node Administration
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IotEventModeRequest"
      responses:
        "200":
          description: Post-flip IoT-rule action mode for both rules
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IotEventModeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/claiming/config:
    post:
      x-feature-flag: assisted-claiming
      summary: Set the assisted-claiming configuration
      description: |
        Stores the claiming configuration - the claiming `mode`, the per-claimant
        quota, the shared X.509 subject, and the CA/leaf validities. This is the
        whole feature's runtime configuration; it is set here rather than at
        deploy time. Every field is optional; omitted fields fall back to
        built-in defaults, and an omitted `mode` leaves claiming off. The `mode`
        and quota take effect on subsequent claims immediately; the subject and
        leaf validity take effect on subsequent certificates; the CA fields apply
        only at the next mint (`POST /v1/admin/claiming/ca`).

        Available only on deployments with assisted claiming enabled. Only super
        admin users can access this endpoint.
      tags:
        - Assisted Claiming
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimConfig"
      responses:
        "200":
          description: Configuration stored
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimConfigResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
    get:
      x-feature-flag: assisted-claiming
      summary: Read the assisted-claiming configuration
      description: |
        Returns the stored claiming configuration and whether the CA has been
        minted. Only super admin users can access this endpoint.
      tags:
        - Assisted Claiming
      security:
        - sigv4: []
      responses:
        "200":
          description: Current configuration and CA status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimConfigResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/claiming/ca:
    post:
      x-feature-flag: assisted-claiming
      summary: Mint or rotate the assisted-claiming CA
      description: |
        Signs and publishes the claiming CA with the KMS signing key, using the
        stored configuration (or defaults). Mint-once: the first call mints and
        a repeat reports the existing CA unchanged with `200`. Pass
        `{"force": true}` to rotate - replacing the published CA, which leaves
        every certificate already issued by the previous CA unverifiable against
        it. There is no delete.

        This is the step that turns claiming on after the claim stacks are
        deployed. Only super admin users can access this endpoint.
      tags:
        - Assisted Claiming
      security:
        - sigv4: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimCaMintRequest"
      responses:
        "200":
          description: CA already present (unchanged)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimCaResponse"
        "201":
          description: CA minted or rotated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimCaResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
    get:
      x-feature-flag: assisted-claiming
      summary: Return the published assisted-claiming CA certificate
      description: |
        Returns the CA certificate PEM once minted. Only super admin users can
        access this endpoint.
      tags:
        - Assisted Claiming
      security:
        - sigv4: []
      responses:
        "200":
          description: The CA certificate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimCaResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: The CA has not been minted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/admin/files/upload-urls:
    post:
      summary: Create upload URL
      description: |
        Gets a pre-signed URL for uploading a file to S3. Currently supports: node certificate uploads, push text message configuration uploads.
      tags:
        - File Management
      security:
        - sigv4: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                file_type:
                  type: string
                  enum: [node_cert, push_text_config]
                  description: Type of file to upload.
                file_name:
                  type: string
                  description: Optional custom file name.
              required:
                - file_type
      responses:
        "201":
          description: Upload session created (URL generated)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetFileUploadUrlResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/integrations/gva:
    post:
      summary: Google Voice Assistant webhook
      description: |
        Handles Google Assistant Smart Home API requests. Processes device synchronization,
        state queries, command execution, and disconnection intents. Requires OAuth access
        token in Authorization header for user authentication.
      tags:
        - Google Voice Assistant
      # authorization_type is NONE (src/user/gva_action/core.py); the Lambda
      # validates the access token itself.
      security:
        - gvaAccessToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GVARequest"
            examples:
              sync:
                value:
                  requestId: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
                  inputs:
                    - intent: "action.devices.SYNC"
              query:
                value:
                  requestId: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
                  inputs:
                    - intent: "action.devices.QUERY"
                      payload:
                        devices:
                          - id: "123"
                            customData:
                              nodeId: "node123"
                              groupId: "group456"
              execute:
                value:
                  requestId: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
                  inputs:
                    - intent: "action.devices.EXECUTE"
                      payload:
                        commands:
                          - devices:
                              - id: "123"
                                customData:
                                  nodeId: "node123"
                                  groupId: "group456"
                            execution:
                              - command: "action.devices.commands.OnOff"
                                params:
                                  on: true
      responses:
        "200":
          description: Successful GVA response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GVAResponse"
              examples:
                sync:
                  value:
                    requestId: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
                    payload:
                      agentUserId: "user123"
                      devices:
                        - id: "123"
                          type: "action.devices.types.LIGHT"
                          traits:
                            - "action.devices.traits.OnOff"
                          name:
                            name: "Living Room Light"
                          willReportState: true
                query:
                  value:
                    requestId: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
                    payload:
                      devices:
                        "123":
                          on: true
                          online: true
                execute:
                  value:
                    requestId: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
                    payload:
                      commands:
                        - ids:
                            - "123"
                          status: "SUCCESS"
                          states:
                            on: true
        "400":
          description: Invalid request format
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: "invalid request format"
        "401":
          description: Unauthorized - Missing or invalid access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: "unauthorized"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: "internal server error"

  # ----------------------------------------------------------------------
  # FILE TEMPLATES (Formats/Schemas)
  # ----------------------------------------------------------------------
  /v1/admin/file-templates/{templateType}:
    get:
      summary: Get file template
      description: |
        Gets the format of a file. Currently only supports push text configuration file format. Returns the default structure/template for the specified file type.
      tags:
        - File Management
      security:
        - sigv4: []
      parameters:
        - in: path
          name: templateType
          required: true
          schema:
            type: string
            enum: [push_text_config]
          description: The ID/Type of the template to retrieve.
      responses:
        "200":
          description: Template retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushTextConfig"
        "404":
          description: Template type not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  # ----------------------------------------------------------------------
  # NODE TAGS (User)
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/nodes/{nodeId}/tags:
    get:
      summary: Get user tags for a node
      description: |
        Returns the user-defined tags for a specific node within the caller's group.
        The caller must have access to the specified group.
      tags:
        - Node Tags
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: User tags retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    type: object
                    additionalProperties: true
                    description: User-defined tags as key-value pairs
              example:
                user:
                  location: "living room"
                  alias: "main light"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    put:
      summary: Update user tags for a node
      description: |
        Creates or updates user-defined tags for a specific node.
        The caller must have access to the specified group.
        Set a tag value to null to delete it.
      tags:
        - Node Tags
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - user
              properties:
                user:
                  type: object
                  additionalProperties: true
                  description:
                    User tags to set. Values can be strings or null (to delete).
            example:
              user:
                location: "living room"
                old_tag: null
      responses:
        "200":
          description: Tags updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # NODE TRIGGER
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/nodes/{nodeId}/triggers:
    get:
      summary: Get node triggers
      description: |
        Retrieves the trigger configuration for a node. Triggers are node-side
        conditions that can feed automations (referenced by IDs of the form
        `<nodeID>~<automationID>~<triggerIndex>`).
        Returns `{"triggers": []}` when no triggers are configured.
      tags:
        - Node Trigger
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeTriggerPayload"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    put:
      summary: Create/Update node triggers
      description: |
        Creates or replaces the trigger configuration for the node. The payload
        must contain a `triggers` array; every entry must have a unique string `id`.
        Writing triggers also bumps the node's trigger version and notifies the
        device over MQTT.
      tags:
        - Node Trigger
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NodeTriggerPayload"
      responses:
        "200":
          description: Triggers updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    delete:
      summary: Delete node triggers
      description: |
        Removes the trigger configuration from the node and notifies the device.
      tags:
        - Node Trigger
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: Triggers deleted successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # GROUP SERVICES — GENERIC DISPATCH
  # ----------------------------------------------------------------------
  # The group service endpoint is a generic dispatcher keyed by {serviceName}.
  # Currently the only registered group service is "automation", documented
  # separately below. Any new group service registered in the service
  # registry is automatically exposed under the same path.

  # ----------------------------------------------------------------------
  # GROUP AUTOMATIONS
  # ----------------------------------------------------------------------
  /v1/groups/{groupId}/service/automations:
    get:
      summary: List automations for a group
      description: |
        Returns every automation configured for the group.
      tags:
        - Group Automation
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListAutomationsResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

    post:
      summary: Create a new automation
      description: |
        Creates a new automation in the group. The server generates a
        3-character automation ID (first character alphabetic) and returns it.
      tags:
        - Group Automation
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AutomationPayload"
      responses:
        "200":
          description: Automation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutomationWriteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    delete:
      summary: Delete all automations for a group
      description: |
        Removes every automation belonging to the group in a single call.
      tags:
        - Group Automation
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
      responses:
        "200":
          description: All automations deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /v1/groups/{groupId}/service/automations/{automationId}:
    get:
      summary: Get an automation by ID
      description: Retrieves a single automation belonging to the group.
      tags:
        - Group Automation
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/AutomationId"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Automation"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Automation not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

    put:
      summary: Update an automation by ID
      description: |
        Updates an existing automation at the given ID, replacing it with the
        supplied definition.
      tags:
        - Group Automation
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/AutomationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AutomationPayload"
      responses:
        "200":
          description: Automation saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    delete:
      summary: Delete an automation by ID
      description: Removes a single automation from the group.
      tags:
        - Group Automation
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/GroupId"
        - $ref: "#/components/parameters/AutomationId"
      responses:
        "200":
          description: Automation deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Automation not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  # ----------------------------------------------------------------------
  # NODE TAGS (Admin)
  # ----------------------------------------------------------------------
  /v1/admin/nodes/{nodeId}/tags:
    get:
      summary: Get all tags for a node (admin)
      description: |
        Returns all tags (admin, device, and user) for a specific node.
        Only super admin users can access this endpoint.
      tags:
        - Node Tags
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: All tags retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  admin:
                    type: object
                    additionalProperties: true
                    description: Admin-defined tags
                  device:
                    type: object
                    additionalProperties: true
                    description: Device-reported tags (read-only)
                  user:
                    type: object
                    additionalProperties: true
                    description: User-defined tags
              example:
                admin:
                  deployment: "beta"
                device:
                  fw_version: "1.2.3"
                user:
                  location: "living room"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    put:
      summary: Update admin and/or user tags for a node (admin)
      description: |
        Creates or updates admin and/or user tags for a specific node.
        Only super admin users can access this endpoint.
        Admin can write both admin and user tags. Set a tag value to null to delete it.
      tags:
        - Node Tags
      security:
        - sigv4: []
      parameters:
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                admin:
                  type: object
                  additionalProperties: true
                  description:
                    Admin tags to set. Values can be strings or null (to
                    delete).
                user:
                  type: object
                  additionalProperties: true
                  description:
                    User tags to set. Values can be strings or null (to delete).
            example:
              admin:
                deployment: "beta"
                old_flag: null
              user:
                location: "kitchen"
      responses:
        "200":
          description: Tags updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

components:
  parameters:
    IntegrationTypeQuery:
      in: query
      name: integration_type
      required: true
      schema:
        type: string
        enum: [apns, apns_sandbox, gcm, alexa, gva]
      description: |
        Integration type for `POST /v1/admin/integrations`.

    IntegrationTypeQueryOptional:
      in: query
      name: integration_type
      required: false
      schema:
        type: string
        enum: [apns, apns_sandbox, gcm, alexa, gva, webhook]
      description: |
        Optional integration type filter. If omitted, all configured
        integrations across all types are returned.

    IntegrationId:
      in: path
      name: integrationId
      required: true
      schema:
        type: string
      description: |
        Opaque server-assigned identifier for one configured integration.
        Returned by `POST /v1/admin/integrations`.

    GroupId:
      in: path
      name: groupId
      required: true
      schema:
        type: string
      description: The unique identifier of the group.

    SubGroupId:
      in: path
      name: subGroupId
      required: true
      schema:
        type: string
      description: The unique identifier of the subgroup.

    AccessTypeQueryOptional:
      in: query
      name: accessType
      required: false
      schema:
        type: string
        enum: [all, primary]
      description: |
        Scopes the subgroup user listing. `all` returns the full membership
        (primary access only); `primary` returns just the group's primary
        owners (available to any subgroup member). If omitted, defaults
        based on the caller's own access level. Case-insensitive.

    NodeId:
      in: path
      name: nodeId
      required: true
      schema:
        type: string
      description: The unique identifier of the node.

    UserId:
      in: path
      name: userId
      required: true
      schema:
        type: string
      description: |
        The unique identifier of the user, as returned by
        `GET /v1/groups/{groupId}/users`. User names (email / phone) are not
        accepted in the path — they are the share-side identifier only, and
        keeping them out of URLs keeps them out of access and CDN logs.

    RequestId:
      in: path
      name: requestId
      required: true
      schema:
        type: string
      description: The unique identifier of the request.

    AutomationId:
      in: path
      name: automationId
      required: true
      schema:
        type: string
      description: |
        The unique identifier of the automation within the group.
        Server-generated IDs are 3 characters long; the first character is
        alphabetic and the remaining characters are lowercase alphanumeric.

    PageSize:
      in: query
      name: page_size
      schema:
        type: integer
        minimum: 1
        default: 20
      description: |
        Maximum number of items to return in a single page. When omitted
        or invalid, the server applies an endpoint-specific default.

    StartKey:
      in: query
      name: start_key
      schema:
        type: string
      description: |
        Pagination cursor. Pass the `next_key` value from a previous
        response to fetch the next page. Omit on the first request.

  schemas:
    AssumeRoleRequest:
      type: object
      properties:
        tags:
          type: object
          additionalProperties:
            type: string
          description: Tags to be associated with the assumed role session

    AssumeRolePerNodeRequest:
      type: object
      required:
        - services
      properties:
        tags:
          type: object
          additionalProperties:
            type: string
          description: Tags to be associated with the assumed role session
        services:
          type: array
          minItems: 1
          items:
            type: string
            enum: [s3, kvs]
          description: |
            Services to scope the returned credentials to, for the single node in the path.
            - "s3": S3 permissions scoped to node-data/{nodeId}/* for that node.
            - "kvs": KVS viewer permissions scoped to channel/rmng-v1-{nodeId}/* for that node.
            The response contains ONLY the listed service permissions — no IoT/MQTT statements.

    AssumeRoleResponse:
      type: object
      required:
        - access_key
        - secret_key
        - session_token
      properties:
        access_key:
          type: string
          description: New AWS access key ID
        secret_key:
          type: string
          description: New AWS secret access key
        session_token:
          type: string
          description: New AWS session token
        expiration:
          type: integer
          format: int64
          nullable: true
          description: Expiration time of the credentials as Unix timestamp
        message:
          type: string

    UserCredsResponse:
      type: object
      properties:
        access_key_id:
          type: string
          description: AWS access key ID
        secret_access_key:
          type: string
          description: AWS secret access key
        session_token:
          type: string
          description: AWS session token
        expiration:
          type: integer
          format: int64
          description: Credential expiration time as Unix timestamp
        message:
          type: string

    SuccessResponse:
      type: object
      properties:
        message:
          type: string
      required:
        - message

    APIStatusMessage:
      type: object
      properties:
        message:
          type: string
      required:
        - message

    Error:
      type: object
      description: |
        Standard API error response.
      properties:
        message:
          type: string
          description: Human-readable error description.
      required:
        - message
      example:
        message: "Invalid request ID"

    ClaimInitiateRequest:
      type: object
      required:
        - mac_addr
      properties:
        mac_addr:
          type: string
          description: |
            Device MAC address. Separators (`:`, `-`, `.`) are stripped and the
            value upper-cased; the normalized result must be 12 or 16
            hexadecimal characters.
          example: "AA:BB:CC:DD:EE:FF"

    ClaimInitiateResponse:
      type: object
      properties:
        node_id:
          type: string
          description: |
            Cloud-assigned node ID - a canonical RFC 4122 version-4 UUID
            (hyphenated lowercase), and also the IoT Thing name, MQTT client ID,
            and certificate Common Name. The device must adopt this before
            installing its certificate. If the node later joins a Matter fabric,
            its Matter operational Node ID is derived from this value; the node ID
            is not itself a Matter Node ID.
          example: "1b4e28ba-2fa1-4d3b-a3f5-ce6f8a9b0c2d"

    ClaimVerifyRequest:
      type: object
      required:
        - mac_addr
        - csr
      properties:
        mac_addr:
          type: string
          description: Device MAC address, normalized as for the initiate call.
          example: "AA:BB:CC:DD:EE:FF"
        csr:
          type: string
          description: |
            PEM-encoded PKCS#10 certificate signing request. Must be ECDSA
            P-256, at most 8 KB, and carry a valid self-signature. Only its
            public key is used; the subject is discarded.
          example: "-----BEGIN CERTIFICATE REQUEST-----\n...\n-----END CERTIFICATE REQUEST-----"
        capabilities:
          type: array
          items:
            type: string
          description: |
            Optional capabilities to apply to the issued certificate's policy
            (e.g. ["s3"]). Must be re-supplied on a re-claim, since they are
            applied to the new certificate.
          example: ["s3"]

    ClaimVerifyResponse:
      type: object
      properties:
        node_id:
          type: string
          description: The reserved node ID the certificate was issued for.
          example: "1b4e28ba-2fa1-4d3b-a3f5-ce6f8a9b0c2d"
        certificate:
          type: string
          description: PEM-encoded device certificate signed by the claiming CA.
        ca_certificate:
          type: string
          description: PEM-encoded claiming CA certificate - the chain to trust.

    ClaimSubject:
      type: object
      description: |
        Shared X.509 subject applied to the CA and every leaf certificate. All
        fields optional; the leaf Common Name is always the node ID and is never
        taken from here.
      properties:
        country:
          type: string
          description: Country (C) - a two-letter ISO 3166 code.
          example: "IN"
        state:
          type: string
          description: State or Province (ST).
          example: "Maharashtra"
        locality:
          type: string
          description: Locality (L).
          example: "Pune"
        organization:
          type: string
          description: Organization (O).
          example: "Espressif Systems"
        organizational_unit:
          type: string
          description: Organizational Unit (OU).
          example: "RainMaker"
        email:
          type: string
          description: emailAddress RDN.
          example: "rainmaker@espressif.com"

    ClaimConfig:
      type: object
      description: |
        Assisted-claiming configuration. This single document is the whole
        feature's runtime configuration — the claiming variant, the per-claimant
        quota, and the certificate identity/validity. Every field is optional; an
        omitted field uses its built-in default, and an omitted `mode` leaves
        claiming configured off (the claim routes fail closed).
      properties:
        mode:
          type: string
          description: |
            Claiming variant. Empty/omitted means claiming is configured off.
            Must be an implemented variant when set.
          enum:
            - user_authenticated
          example: "user_authenticated"
        max_nodes_per_claimant:
          type: integer
          description: |
            Per-caller lifetime node reservation quota for the user_authenticated
            variant. 0 (or omitted) uses the built-in default (20). Must not be
            negative.
          example: 20
        subject:
          $ref: "#/components/schemas/ClaimSubject"
        ca_common_name:
          type: string
          description: |
            CA certificate Common Name. Default: derived from the signing key's
            account and region.
          example: "Espressif RainMaker Claiming CA"
        ca_validity_years:
          type: integer
          description: "CA lifetime in years. Default: leaf validity + 20."
          example: 30
        leaf_validity_years:
          type: integer
          description: "Device (leaf) certificate lifetime in years. Default: 100."
          example: 10

    ClaimConfigResponse:
      type: object
      properties:
        config:
          $ref: "#/components/schemas/ClaimConfig"
        ca_minted:
          type: boolean
          description: Whether the CA certificate has been minted yet.

    ClaimCaMintRequest:
      type: object
      properties:
        force:
          type: boolean
          description: |
            Rotate an existing CA by overwriting it. Omitted/false is mint-once
            (an existing CA is left unchanged). Rotation leaves certificates
            issued by the previous CA unverifiable against the published one.
          example: false

    ClaimCaResponse:
      type: object
      properties:
        minted:
          type: boolean
          description: True when this call minted (or rotated) the CA.
        common_name:
          type: string
          description: The CA certificate Common Name.
          example: "Espressif RainMaker Claiming CA"
        ca_certificate:
          type: string
          description: PEM-encoded claiming CA certificate.

    CreateGroupRequest:
      type: object
      properties:
        group_name:
          type: string
          description: The name of the group to create
        capabilities:
          type: array
          items:
            type: string
            enum: [matter]
          description:
            Optional list of capabilities to enable for this group (e.g.,
            ["matter"])

    CreateGroupResponse:
      type: object
      properties:
        group_id:
          type: string
          description: The unique identifier for the created group
        matter:
          $ref: "#/components/schemas/MatterCapabilityResponse"
        message:
          type: string

    AddGroupCapabilitiesRequest:
      type: object
      required:
        - capabilities
      properties:
        capabilities:
          type: array
          description: Capabilities to enable on the group (e.g. ["matter"])
          minItems: 1
          items:
            type: string
          example: ["matter"]

    AddGroupCapabilitiesResponse:
      type: object
      properties:
        matter:
          $ref: "#/components/schemas/MatterCapabilityResponse"

    MatterCapabilityResponse:
      type: object
      description:
        Matter fabric capability data (present when group has matter capability)
      properties:
        fabric_id:
          type: string
          description:
            16-character hex string representing the Matter Fabric ID (derived
            from group_id)
          example: "6162633132330000"
        root_ca:
          type: string
          description: PEM-encoded Root CA certificate for the Matter fabric
          example: "-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"
        ipk:
          type: string
          description:
            32-character hex string representing the Identity Protection Key
          example: "0123456789ABCDEF0123456789ABCDEF"
        group_cat_id_admin:
          type: string
          description:
            8-character hex string for admin CAT (Certification Authority Tag)
            ID
          example: "01000001"
        group_cat_id_operate:
          type: string
          description:
            8-character hex string for operate CAT (Certification Authority Tag)
            ID
          example: "06000001"
        message:
          type: string

    MatterNOCRequest:
      type: object
      required:
        - csr
      properties:
        csr:
          type: string
          description:
            PEM-encoded Certificate Signing Request (CSR) generated on the
            client device
          example:
            "-----BEGIN CERTIFICATE
            REQUEST-----\nMIIBVDCB+wIBADBFMQ...\n-----END CERTIFICATE
            REQUEST-----"

    MatterNOCResponse:
      type: object
      required:
        - noc
        - matter_node_id
      properties:
        noc:
          type: string
          description:
            PEM-encoded Node Operational Certificate (NOC) signed by the group's
            Root CA
          example:
            "-----BEGIN CERTIFICATE-----\nMIIBpjCCAUygAwIBAgIIX...\n-----END
            CERTIFICATE-----"
        matter_node_id:
          type: string
          description:
            Canonical 16-character Matter Node ID derived from the authenticated
            rmng user, fabric, and CSR operational public key
          example: "FEDCBA0987654321"
        message:
          type: string

    ListGroupsResponse:
      type: object
      properties:
        groups:
          type: array
          items:
            $ref: "#/components/schemas/GroupInfo"
        message:
          type: string

    ListGroupUsersResponse:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/GroupUserInfo"
        message:
          type: string

    GroupUserInfo:
      type: object
      required:
        - user_id
        - access_type
      properties:
        user_id:
          type: string
          description: The unique identifier of the user
        email:
          type: string
          description:
            The user's email address (may be empty if the user registered with
            phone only)
        phone_number:
          type: string
          description:
            The user's phone number (may be empty if the user registered with
            email only)
        access_type:
          type: string
          enum: [primary, secondary, subgroup]
          description: The level of access this user has to the group
        subgroups:
          type: array
          items:
            type: string
          description:
            Subgroup IDs this user has access to. Only present for subgroup
            access type.
        message:
          type: string

    GroupInfo:
      type: object
      properties:
        group_id:
          type: string
          description: The unique identifier for the group
        group_name:
          type: string
          description: The name of the group
        access_type:
          type: string
          enum: [primary, secondary, subgroup]
          description: The user's access level for this group
        node_ids:
          type: array
          items:
            type: string
          description: List of node IDs associated with this group
        node_details:
          type: object
          description: Capability information for nodes in the group
          additionalProperties:
            $ref: "#/components/schemas/NodeCapabilityInfo"
        subgroups:
          type: array
          items:
            $ref: "#/components/schemas/SubGroupInfo"
        matter:
          $ref: "#/components/schemas/MatterCapabilityResponse"
        message:
          type: string

    NodeCapabilityInfo:
      type: object
      properties:
        capabilities:
          type: array
          description: >
            Capability names for this node. "rmng" denotes a RainMaker node and
            is present for every node except pure Matter nodes; "matter" denotes
            a node on the group's Matter fabric; other names (e.g. "acc") are
            feature capabilities.
          items:
            type: string
            enum:
              - rmng
              - matter
              - acc
          example: ["rmng", "matter"]
        capability_details:
          type: object
          description: Map of capability name to its detailed data.
          additionalProperties:
            type: object
          example:
            acc:
              endpoint_id: "de152ff2-f070-4d0e-94da-770828b1770f"
            matter:
              matter_node_id: "B2C3D4E5F6071829"
        message:
          type: string

    SubGroupInfo:
      type: object
      properties:
        subgroup_id:
          type: string
          description: The unique identifier for the subgroup
        subgroup_name:
          type: string
          description: The name of the subgroup
        node_ids:
          type: array
          items:
            type: string
          description: List of node IDs associated with this subgroup
        message:
          type: string

    CreateSubgroupRequest:
      type: object
      properties:
        subgroup_name:
          type: string
          description: The name of the subgroup to create

    CreateSubgroupResponse:
      type: object
      properties:
        subgroup_id:
          type: string
          description: The unique identifier for the created subgroup
        message:
          type: string

    UpdateGroupRequest:
      type: object
      required:
        - group_name
      properties:
        group_name:
          type: string
          description: The new name for the group

    UpdateSubgroupRequest:
      type: object
      required:
        - subgroup_name
      properties:
        subgroup_name:
          type: string
          description: The new name for the subgroup

    ListSharingRequestsResponse:
      type: object
      properties:
        sharing_requests:
          type: array
          items:
            $ref: "#/components/schemas/SharingRequestInfo"
        message:
          type: string

    SharingRequestInfo:
      type: object
      properties:
        sharing_request_id:
          type: string
        group_id:
          type: string
        subgroup_id:
          type: string
        access_type:
          type: string
        primary_user_id:
          type: string
          description: User ID of the user who created the sharing request
        primary_email:
          type: string
          description: Email of the user who created the sharing request
        primary_phone_number:
          type: string
          description: Phone number of the user who created the sharing request
        message:
          type: string

    CreateSharingRequestRequest:
      type: object
      required:
        - username
      properties:
        username:
          type: string
          description: |
            User name of the invitee — the email address or E.164 phone number
            they sign in with. Internal user IDs are not accepted here.
            Matched exactly as the account was registered, so the case must
            match for email.
          example: "invitee@example.com"
        access_type:
          type: string
          enum: [primary, secondary]
          description: |
            Access level to grant. Defaults to `secondary` if omitted.
            Ignored when sharing a subgroup.

    RegisterEndpointRequest:
      type: object
      required:
        - delivery_credentials
      properties:
        delivery_credentials:
          type: object
          description: |
            Credentials used to deliver notifications to this user on this
            integration. Shape depends on `integrationType`:
            `{ "app_token": "..." }` for push integrations (apns,
            apns_sandbox, gcm and their mock variants), or an OAuth bundle
            (`{ "access_token", "refresh_token", "expires_at" }`) for
            account-link integrations (alexa, gva) and webhook integrations.
          properties:
            app_token:
              type: string
              description: |
                Raw push token issued by the OS (APNS / GCM). Required for
                push integrations; absent otherwise.
            access_token:
              type: string
              description:
                OAuth access token. Account-link / webhook integrations only.
            refresh_token:
              type: string
              description:
                OAuth refresh token. Account-link / webhook integrations only.
            expires_at:
              type: integer
              description: Unix timestamp at which `access_token` expires.
        locale:
          type: string
          description: "Locale code (e.g., en_US)"
          example: "en_US"

    RegisterEndpointResponse:
      type: object
      required:
        - status
        - endpoint_id
      properties:
        status:
          type: string
          enum: [success]
        endpoint_id:
          type: string
          description:
            Opaque identifier for the registered endpoint. Today the (user_id,
            integration_id) pair is 1:1 so endpoint_id equals integration_id,
            but callers should treat it as opaque so the contract survives the
            eventual move to multi-endpoint per user.

    GroupEditResponse:
      type: object
      properties:
        message:
          type: string
      required:
        - message

    GetNodeResponse:
      type: object
      properties:
        node_id:
          type: string
          description: ID of the node
        config:
          type: object
          additionalProperties: true
          description:
            Node configuration object containing device-specific settings
        message:
          type: string

    InitiateResponse:
      type: object
      properties:
        request_id:
          type: string
          description: Unique identifier for this association request
        challenge:
          type: string
          description: |
            Challenge string that must be signed by the node.
            For Matter-capable groups: 64 uppercase hex characters (32 bytes) to be used as CSR nonce.
            For non-Matter groups: Alphanumeric challenge string.
        message:
          type: string
      required:
        - request_id
        - challenge
      example:
        request_id: "abc123def456"
        challenge: "A1B2C3D4E5F6A1B2C3D4E5F6A1B2C3D4E5F6A1B2C3D4E5F6A1B2C3D4E5F6A1B2"

    VerifyRequest:
      type: object
      description: |
        Two authentication methods are supported (mutually exclusive - exactly one must be used):

        **Method 1: challenge_response** - Traditional signature verification.
        Requires `challenge_response` and `node_id`. Works for both Matter and non-Matter groups.
        The node is immediately added to the group. No NOC is generated.

        **Method 2: nocsr_elements** - Matter attestation with NOC generation.
        Requires `nocsr_elements`, `attestation_challenge`, and `attestation_signature`.
        Only valid for Matter-capable groups. Returns a device NOC. Call /confirm after
        the device installs the NOC.
      properties:
        challenge_response:
          type: string
          description: |
            Hex-encoded signature of the SHA256 hash of the challenge, signed by the node's private key.
            **Mutually exclusive with nocsr_elements** - exactly one must be provided.
        node_id:
          type: string
          description: |
            The ID of the node to be associated.
            **Required when using challenge_response.**
            **Optional/ignored when using nocsr_elements** - the node_id is extracted from
            vendor_reserved1 or auto-generated for pure Matter nodes (16-character hex string, 8 bytes).
        nocsr_elements:
          type: string
          description: |
            Hex-encoded NOCSRElements TLV structure for Matter attestation verification.
            **Only valid for Matter-capable groups.**
            **Mutually exclusive with challenge_response** - exactly one must be provided.

            Contains CSR (extracted for NOC generation), CSRNonce, and optional vendor_reserved
            fields. The CSRNonce must match the challenge from the /initiate step.

            If vendor_reserved1 contains a nodeID (thing name), the attestation signature
            will be verified against registered certificates for that node. If no certificates
            are found, or if vendor_reserved1 is not present, the device is treated as a
            pure Matter node and a random 16-character hex node_id (8 bytes) is generated.

            The TLV structure format:
            - Tag 0x01: CSR (DER-encoded Certificate Signing Request)
            - Tag 0x02: CSRNonce (must match the 32-byte challenge from /initiate)
            - Tag 0x03: vendor_reserved1 (optional, contains nodeID/thing name)
            - Tag 0x04: vendor_reserved2 (optional)
            - Tag 0x05: vendor_reserved3 (optional)
          example: "1530010102..."
        attestation_challenge:
          type: string
          description: |
            Hex-encoded 16-byte attestation challenge from PASE/CASE session.
            **Required when nocsr_elements is provided.**
            Used together with NOCSRElements to compute the TBS (to-be-signed):
            SHA256(NOCSRElements || AttestationChallenge).
          example: "0102030405060708090a0b0c0d0e0f10"
        attestation_signature:
          type: string
          description: |
            Hex-encoded 64-byte ECDSA signature in raw r||s format (32 bytes r, 32 bytes s).
            This is the signature over SHA256(NOCSRElements || AttestationChallenge).
            **Required when nocsr_elements is provided.**

            For devices with registered certificates (nodeID found in vendor_reserved1),
            this signature is verified against the device's DAC public key.
            For pure Matter devices (no vendor_reserved1 or no certificates), signature
            verification is skipped.
          example: "abcd1234...64byteshex"

    VerifyMatterResponse:
      type: object
      description: Response for Matter group verification (includes device NOC)
      properties:
        noc:
          type: string
          description: PEM-encoded Node Operational Certificate for the device
          example:
            "-----BEGIN CERTIFICATE-----\nMIIBpjCCAUygAwIBAgIIX...\n-----END
            CERTIFICATE-----"
        matter_node_id:
          type: string
          description: |
            16-character hex string representing the device's Matter Node ID within this fabric.
            Derived from the node_id (thing name) using ASCII-to-hex encoding.
          example: "4E6F64653132330000000000"
        node_id:
          type: string
          description: |
            RainMaker node ID (thing name). For pure Matter nodes without vendor_reserved1,
            this is a randomly generated 16-character (8-byte) hex string. For RainMaker
            devices with vendor_reserved1, this matches the device's thing name.
          example: "MySmartLight01"
        message:
          type: string
      required:
        - noc
        - matter_node_id
        - node_id

    IntegrationConfigRequest:
      description: |
        Configuration body for registering or updating an integration. The
        variant used is selected by the `integration_type` query parameter
        (on POST) or by the integration's stored type (on PUT).
      oneOf:
        - $ref: "#/components/schemas/PushApnsConfigRequest"
        - $ref: "#/components/schemas/PushGcmConfigRequest"
        - $ref: "#/components/schemas/AlexaCfgRequest"
        - $ref: "#/components/schemas/GVACfgRequest"

    IntegrationConfigResponse:
      description: |
        Response body for `GET /v1/admin/integrations/{integrationId}`.
        Shape matches the integration's type.
      oneOf:
        - $ref: "#/components/schemas/PushApnsConfigResponse"
        - $ref: "#/components/schemas/PushGcmConfigResponse"
        - $ref: "#/components/schemas/AlexaCfgGetResponse"
        - $ref: "#/components/schemas/GVACfgGetResponse"

    RegisterIntegrationResponse:
      type: object
      required:
        - integration_id
      properties:
        integration_id:
          type: string
          description: |
            Opaque identifier for the newly registered integration. Pass
            back as the `integrationId` path parameter on subsequent
            operations.

    ListIntegrationsResponse:
      type: object
      required:
        - integrations
      properties:
        integrations:
          type: array
          items:
            $ref: "#/components/schemas/IntegrationConfigResponse"
          description: |
            Array of configured integrations. Each item is the full
            per-type detail (same shape as `GET /v1/admin/integrations/{integrationId}`).

    PublicIntegrationSummary:
      type: object
      required:
        - integration_id
        - integration_type
      properties:
        integration_id:
          type: string
          description: |
            Identifier to pass as the `integrationId` path parameter when
            registering a delivery endpoint.
          example: "apns_sandbox_com.company.app"
        integration_type:
          type: string
          enum: [apns, apns_sandbox, gcm, alexa, gva, webhook]
          example: "apns_sandbox"

    ListPublicIntegrationsResponse:
      type: object
      required:
        - integrations
      properties:
        integrations:
          type: array
          items:
            $ref: "#/components/schemas/PublicIntegrationSummary"
          description: |
            Array of configured integrations as the non-admin id+type summary
            (no credentials). Returned by `GET /v1/integrations`.

    PushApnsConfigRequest:
      type: object
      required:
        - authentication_key
        - key_id
        - team_id
        - bundle_id
      properties:
        authentication_key:
          type: string
          description: APNS token-based auth key (.p8 key file contents).
        key_id:
          type: string
          description: APNS signing key ID from Apple Developer account.
        team_id:
          type: string
          description: APNS team ID from Apple Developer account.
        bundle_id:
          type: string
          description: APNS bundle identifier (used to derive `integration_id`).
        apns_sandbox:
          type: boolean
          default: false
          description: |
            If true, the integration is created against the APNS sandbox
            (development) environment. Use `integration_type=apns_sandbox`
            in the query parameter to make this explicit.

    PushApnsConfigResponse:
      type: object
      required:
        - integration_id
        - integration_type
        - key_id
        - team_id
        - bundle_id
      properties:
        integration_id:
          type: string
          example: "apns_com.company.app"
        integration_type:
          type: string
          enum: [apns, apns_sandbox]
        key_id:
          type: string
        team_id:
          type: string
        bundle_id:
          type: string

    PushGcmConfigRequest:
      description: |
        Google Cloud service-account JSON for Firebase Cloud Messaging.
        Shape mirrors GVA (`GVACfgRequest`) — both endpoints accept the
        same standard Google service-account key file. `project_id`
        becomes the derived `integration_id` suffix.
      allOf:
        - $ref: "#/components/schemas/GoogleServiceAccountConfig"

    PushGcmConfigResponse:
      description: |
        Full GCM integration detail: the stored Google service-account
        fields (mirrors `GVACfgGetResponse`) minus the write-only
        `private_key`, which is never returned.
      allOf:
        - type: object
          required:
            - integration_id
            - integration_type
          properties:
            integration_id:
              type: string
              example: "gcm_my-firebase-project"
            integration_type:
              type: string
              enum: [gcm]
        - $ref: "#/components/schemas/GoogleServiceAccountResponse"

    AlexaCfgRequest:
      type: object
      required:
        - redirect_uris
        - client_id
        - client_secret
        - skill_id
      properties:
        redirect_uris:
          type: array
          items:
            type: string
          description:
            List of redirect URIs to register on the voice-assistant OIDC client
            client
        client_id:
          type: string
          description: Alexa Skill client ID to store in SSM Parameter Store
        client_secret:
          type: string
          description: Alexa Skill client secret to store in SSM Parameter Store
        skill_id:
          type: string
          description:
            Alexa Skill ID to add as a trigger to the Alexa Skill Lambda
            function
        manufacturer_name:
          type: string
          description: |
            Brand advertised in the Alexa discovery response, which Works With Alexa
            review requires to be a real manufacturer. Optional: omit it to leave the
            stored brand untouched (so a credentials-only update does not reset an
            OEM's branding), or send it empty to restore the default brand
            ("Espressif"). A manufacturer reported by the device itself
            (`info.manufacturer` in its node config) takes precedence over this value.
          example: "Espressif"

    AlexaCfgResponse:
      type: object
      properties:
        message:
          type: string
      required:
        - message

    AlexaCfgGetResponse:
      type: object
      properties:
        client_id:
          type: string
          description: Alexa Skill client ID
        skill_id:
          type: string
          description: Alexa Skill ID
        redirect_uris:
          type: array
          items:
            type: string
          description: OAuth redirect URIs registered on the voice-assistant OIDC client
        manufacturer_name:
          type: string
          description:
            Brand currently advertised in discovery — the configured value, or the
            default brand when the deployment has set none. Never empty.
          example: "Espressif"
        message:
          type: string

    GetFileUploadUrlResponse:
      type: object
      required:
        - upload_url
        - s3_path
      properties:
        upload_url:
          type: string
          description: Pre-signed URL for uploading the file to S3
        s3_path:
          type: string
          description: S3 path where the file will be stored
        message:
          type: string

    PushTextConfig:
      type: object
      description: |
        Push-notification text configuration. The response contains a
        `default` (locale-independent) block and an optional `locale` map
        keyed by locale code (e.g. `en_US`).
      properties:
        default:
          $ref: "#/components/schemas/PushTextLocale"
        locale:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/PushTextLocale"
        message:
          type: string

    PushTextLocale:
      type: object
      properties:
        title:
          type: string
          description: Common title for push notifications
          example: "ESP RainMaker"
        event:
          type: object
          description: Event-specific push text configurations
          additionalProperties:
            $ref: "#/components/schemas/PushTextForEvent"
          example:
            device_alert:
              text: "Device {nodeID} has an alert!"
        message:
          type: string

    PushTextForEvent:
      type: object
      properties:
        title:
          type: string
          description:
            Event-specific title (optional, falls back to common title)
        text:
          type: string
          description: Text message for the event, supports {nodeID} placeholder
          example: "Device {nodeID} has an alert!"
        priority:
          type: string
          enum: [low, normal, high]
          description: Priority hint for the push message.
        message:
          type: string

    RegisterAppPlatformRequest:
      type: object
      required:
        - platform
      properties:
        platform:
          type: string
          enum: [APNS, GCM]
          description: The mobile platform type (APNS for iOS, GCM for Android)
        authentication_key:
          type: string
          description:
            APNS token-based authentication key (.p8 key content) - required for
            APNS
        key_id:
          type: string
          description:
            APNS signing key ID from Apple Developer account - required for APNS
        team_id:
          type: string
          description:
            APNS team ID from Apple Developer account - required for APNS
        bundle_id:
          type: string
          description: APNS bundle identifier - required for APNS
        api_key:
          type: string
          description: GCM API key (JSON service account) - required for GCM
        apns_sandbox:
          type: boolean
          description:
            For APNS - true for sandbox environment, false for production
          default: false

    RegisterAppPlatformResponse:
      type: object
      required:
        - platform_app_arn
        - platform_name
        - app_platform_id
      properties:
        platform_app_arn:
          type: string
          description: The ARN of the created SNS platform application
        platform_name:
          type: string
          description:
            The name of the platform application (bundle ID for APNS, project ID
            for GCM)
        app_platform_id:
          type: string
          description: |
            The unique identifier for the application platform.
            Format: `{platform_type}` or `{platform_type}_{platform_app_name}`
            Examples: `APNS_com.example.myapp`, `APNS_SANDBOX_com.example.myapp`, `GCM_my-project-id`
        message:
          type: string

    ListAppPlatformsResponse:
      type: object
      required:
        - platforms
      properties:
        platforms:
          type: array
          items:
            $ref: "#/components/schemas/AppPlatformInfo"
          description: Array of configured mobile platforms
        message:
          type: string

    IotEventModeRequest:
      type: object
      required:
        - mode
      properties:
        mode:
          type: string
          enum: [direct, sqs]
          description: |
            Target IoT-rule action mode applied to both `node_disconnected_rule`
            and `node_to_cloud_rule`. `direct` makes each rule invoke its
            lambda directly; `sqs` makes each rule write to its SQS queue,
            which the lambda then consumes in batches.

    IotEventModeResponse:
      type: object
      required:
        - presence
        - publish_input
      properties:
        presence:
          type: string
          enum: [direct, sqs]
          description: Current action mode of `node_disconnected_rule`.
        publish_input:
          type: string
          enum: [direct, sqs]
          description: Current action mode of `node_to_cloud_rule`.

    UpdateAppPlatformRequest:
      type: object
      properties:
        authentication_key:
          type: string
          description:
            APNS token-based authentication key (.p8 key content) - required for
            APNS
        key_id:
          type: string
          description:
            APNS signing key ID from Apple Developer account - required for APNS
        team_id:
          type: string
          description:
            APNS team ID from Apple Developer account - required for APNS
        bundle_id:
          type: string
          description: APNS bundle identifier - required for APNS
        api_key:
          type: string
          description: GCM API key (JSON service account) - required for GCM

    AppPlatformInfo:
      type: object
      properties:
        platform:
          type: string
          description: The platform type (APNS, APNS_SANDBOX, GCM, etc.)
        platform_app_name:
          type: string
          description: The application name for this platform
        message:
          type: string
      example:
        platform: "APNS"
        platform_app_name: "RainMaker"

    RegisterNodeStatusResponse:
      type: object
      properties:
        request_id:
          type: string
          description: The request ID of the bulk node registration job
        user_id:
          type: string
          description: The user who initiated the request
        job_type:
          type: string
          enum: [register, update]
          description: |
            Which bulk-job flow this row belongs to. Rows created before
            this field was introduced default to `register` for backward
            compatibility.
        total_nodes:
          type: integer
          description: Total number of nodes in the request
        success_count:
          type: integer
          description: Number of nodes successfully registered
        failed_count:
          type: integer
          description: Number of nodes that failed to register
        created_at:
          type: integer
          description: Request Creation timestamp (epoch seconds)
        last_updated_at:
          type: integer
          description: Last updated timestamp (epoch seconds)
        status:
          type: string
          description:
            Current status of the request (requested, started, completed)
        message:
          type: string
          description: Status or error message
        admin_group_names:
          type: array
          items:
            type: string
          description: Admin group names assigned by the job
        admin_parent_group_name:
          type: string
          description: Parent group name for admin groups
        tags:
          type: array
          items:
            type: string
          description: Tags applied by the job (key:value format)
        cert_file_s3_path:
          type: string
          description:
            S3 path to the certificate CSV file used for bulk registration
        failed_file_s3_path:
          type: string
          description: |
            S3 path of the re-uploadable, cert-bearing CSV of failed rows,
            written by the job at completion. Present only when the job
            finished with at least one failure (and the S3 write succeeded).
            This key is itself a valid `cert_file_s3_path` for a retry job.
        failed_file_download_url:
          type: string
          description: |
            Short-lived presigned GET URL for `failed_file_s3_path`,
            regenerated on every status call. Fetch the CSV directly from
            S3 with this URL (bypasses the API response payload limit).
            Present only when `failed_file_s3_path` is set.

    ListRegistrationJobsResponse:
      type: object
      properties:
        jobs:
          type: array
          items:
            $ref: "#/components/schemas/RegisterNodeStatusResponse"
          description: List of registration jobs
        page_total:
          type: integer
          description: Number of jobs in the current page
        next_key:
          type: string
          description:
            Pagination token for the next page of results (absent if no more
            pages)
        message:
          type: string
      example:
        jobs:
          - request_id: "abc123"
            status: "completed"
            total_nodes: 50
            success_count: 50
            failed_count: 0
            created_at: 1704067200
            last_updated_at: 1704067260
        page_total: 1
        next_key: "eyJyZXF1ZXN0X2lkIjp7InZhbHVlIjoiYWJjMTIzIiwidHlwZSI6IlMifSwiZ3NpX3BrIjp7InZhbHVlIjoiQUxMIiwidHlwZSI6IlMifSwiY3JlYXRlZF9hdCI6eyJ2YWx1ZSI6IjE3MDQwNjcyMDAiLCJ0eXBlIjoiTiJ9fQ"

    TimeseriesServiceInfoResponse:
      type: object
      properties:
        service:
          type: string
          enum: [timeseries]
          description: Service name
        parameters:
          type: object
          additionalProperties: true
          description: Description of available query parameters
        raw_data:
          type: object
          additionalProperties: true
          description: Parameters specific to raw data queries
        aggregates:
          type: object
          additionalProperties: true
          description: Parameters specific to aggregates queries
        examples:
          type: object
          additionalProperties: true
          description: Example API calls for different query types
        message:
          type: string
      example:
        service: "timeseries"
        parameters:
          key:
            "Data point key (required) — e.g. `Sensor.Temperature` or
            `0x1.c.s.0x402.a.0x0`"
          data_type: "Data type (required)"
          type: "Data type: raw (default), latest, aggregates"

    TimeseriesRawDataResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/TimeseriesDataPoint"
          description: Array of raw timeseries data points
        page_total:
          type: integer
          description: Number of items in the current page
        next_key:
          type: string
          description:
            Pagination token for the next page of results (absent if no more
            pages)
        message:
          type: string
      example:
        data:
          - key: "temperature"
            dt: "float"
            ts: 1640995200000
            value: 23.5
            tz: "UTC"
            cumulative: false
        page_total: 1
        next_key: "eyJub2RlX3BhcmFtX2R0Ijp7InZhbHVlIjoiYWJjLnRlbXBlcmF0dXJlLmZsb2F0IiwidHlwZSI6IlMifSwidHMiOnsidmFsdWUiOiIxNjQwOTk1MjAwMDAwIiwidHlwZSI6Ik4ifX0"

    TimeseriesLatestDataResponse:
      type: object
      properties:
        data:
          $ref: "#/components/schemas/TimeseriesDataPoint"
          description: The single latest data point object
        message:
          type: string
      required:
        - data
      example:
        data:
          key: "temperature"
          dt: "float"
          ts: 1640995200000
          value: 23.5
          tz: "UTC"
          cumulative: false

    TimeseriesAggregatesResponse:
      type: object
      properties:
        aggregates:
          type: array
          items:
            $ref: "#/components/schemas/TimeseriesAggregateData"
          description: Array of aggregated timeseries data
        page_total:
          type: integer
          description: Number of items in the current page (range queries only)
        next_key:
          type: string
          description:
            Pagination token for the next page of results (absent if no more
            pages)
        query_info:
          type: object
          properties:
            parameter:
              type: string
              description: Full parameter identifier
            window_type:
              type: string
              description: Window type used for aggregation
            start_date:
              type: string
              description: Start date for range queries
            end_date:
              type: string
              description: End date for range queries
            message:
              type: string
              description: Informational message if no data available
          description: Query information and metadata
        message:
          type: string
      example:
        aggregates:
          - parameter: "node123.temperature.float"
            is_cumulative: false
            windows:
              hourly:
                count: 24
                sum: 564.0
                min: 20.5
                max: 26.8
                average: 23.5
        page_total: 1
        next_key: "eyJub2RlX3BhcmFtX2R0Ijp7InZhbHVlIjoibm9kZTEyMy50ZW1wZXJhdHVyZS5mbG9hdCIsInR5cGUiOiJTIn0sImludGVydmFsX2tleSI6eyJ2YWx1ZSI6ImRhaWx5IzIwMjUtMDEtMTUiLCJ0eXBlIjoiUyJ9fQ"

    TimeseriesDataPoint:
      type: object
      properties:
        key:
          type: string
          description: Parameter key
          example: "temperature"
        dt:
          type: string
          description: Data type
          example: "float"
        ts:
          type: integer
          format: int64
          description: Timestamp in milliseconds
          example: 1640995200000
        value:
          oneOf:
            - type: number
            - type: integer
            - type: boolean
            - type: string
          description: Parameter value (type depends on data_type)
          example: 23.5
        tz:
          type: string
          description:
            Timezone identifier (IANA name, e.g. "UTC", "Asia/Kolkata")
          example: "UTC"
        cumulative:
          type: boolean
          description: Whether this parameter is cumulative
          example: false
        message:
          type: string
      required:
        - key
        - dt
        - ts
        - value
        - cumulative

    TimeseriesAggregateData:
      type: object
      properties:
        parameter:
          type: string
          description: Full parameter identifier (nodeId.paramName.dataType)
          example: "node123.temperature.float"
        is_cumulative:
          type: boolean
          description: Whether this parameter is cumulative
          example: false
        windows:
          type: object
          description:
            Aggregated data for different time windows (for current aggregates)
          properties:
            hourly:
              $ref: "#/components/schemas/TimeseriesWindowAggregates"
            daily:
              $ref: "#/components/schemas/TimeseriesWindowAggregates"
            weekly:
              $ref: "#/components/schemas/TimeseriesWindowAggregates"
            monthly:
              $ref: "#/components/schemas/TimeseriesWindowAggregates"
          additionalProperties: false
        window_type:
          type: string
          enum: [hourly, daily, weekly, monthly]
          description: Window type for historical aggregates
          example: "daily"
        date:
          type: string
          description: Date for historical aggregates
          example: "2024-01-15"
        count:
          type: integer
          description: Number of data points in the aggregate
          example: 24
        sum:
          type: number
          description: Sum of all values
          example: 564.0
        min:
          type: number
          description: Minimum value
          example: 20.5
        max:
          type: number
          description: Maximum value
          example: 26.8
        average:
          type: number
          description: Average value
          example: 23.5
        first_value:
          oneOf:
            - type: number
            - type: integer
            - type: boolean
            - type: string
          description: First value in the time window
          example: 22.0
        last_value:
          oneOf:
            - type: number
            - type: integer
            - type: boolean
            - type: string
          description: Last value in the time window
          example: 24.0
        cumulative_value:
          type: number
          description: Cumulative value (for cumulative parameters)
          example: 564.0
        window_start:
          type: string
          format: date-time
          description: Start time of the aggregation window
          example: "2024-01-15T00:00:00Z"
        window_end:
          type: string
          format: date-time
          description: End time of the aggregation window
          example: "2024-01-16T00:00:00Z"
        status:
          type: string
          enum: [completed, processing, error]
          description: Status of the aggregation
          example: "completed"
        message:
          type: string
          description: Message when no data is available
          example: "No historical data available for this window"

    TimeseriesWindowAggregates:
      type: object
      properties:
        count:
          type: integer
          description: Number of data points
          example: 24
        sum:
          type: number
          description: Sum of all values
          example: 564.0
        min:
          type: number
          description: Minimum value
          example: 20.5
        max:
          type: number
          description: Maximum value
          example: 26.8
        average:
          type: number
          description: Average value
          example: 23.5
        first_value:
          oneOf:
            - type: number
            - type: integer
            - type: boolean
            - type: string
          description: First value in the time window
          example: 22.0
        last_value:
          oneOf:
            - type: number
            - type: integer
            - type: boolean
            - type: string
          description: Last value in the time window
          example: 24.0
        cumulative_value:
          type: number
          description: Cumulative value (for cumulative parameters)
          example: 564.0
        window_start:
          type: integer
          format: int64
          description: Start timestamp of the window
          example: 1640995200
        window_end:
          type: integer
          format: int64
          description: End timestamp of the window
          example: 1641081600
        status:
          type: string
          enum: [completed, processing, error]
          description: Status of the aggregation
          example: "completed"
        message:
          type: string

    # --- Single Node Schemas ---
    RegisterSingleNodeRequest:
      type: object
      required: [cert]
      properties:
        cert:
          type: string
          description: The PEM-encoded certificate for the node.
        checksum:
          type: string
          pattern: "^[a-fA-F0-9]{32}$"
          description: MD5 checksum of the certificate.
        ca_cert:
          type: string
          description: The PEM-encoded CA certificate (optional).
        ca_checksum:
          type: string
          pattern: "^[a-fA-F0-9]{32}$"
          description: MD5 checksum of `ca_cert` (optional).
        admin_group_names:
          type: array
          items:
            type: string
          description: List of group names to add the node to.
        admin_parent_group_name:
          type: string
          description:
            Name of the parent group under which admin groups should be created.
        tags:
          type: array
          items:
            type: string
          description: List of tags to associate with the node.
        capabilities:
          type: array
          items:
            type: string
            enum: [s3, kvs]
          description: |
            Optional list of device capabilities to enable during registration.
            - "s3": Attaches DeviceFilePolicy for S3 file access via IoT Credential Provider.
            - "kvs": Attaches DeviceVideoPolicy for KVS streaming via IoT Credential Provider.
            If omitted, only the DefaultThingPolicy (MQTT/shadow) is attached.

    RegisterSingleNodeResponse:
      type: object
      required:
        - node_id
      properties:
        node_id:
          type: string
          description: The ID of the newly created node.
        message:
          type: string

    # --- Bulk Node Schemas ---
    RegisterBulkNodesRequest:
      type: object
      required: [cert_file_s3_path]
      properties:
        cert_file_s3_path:
          type: string
          description: S3 path to the CSV file (must start with 's3://').
        admin_group_names:
          type: array
          items:
            type: string
          description: List of group names to add ALL nodes to.
        admin_parent_group_name:
          type: string
          description:
            Name of the parent group under which admin groups should be created.
        tags:
          type: array
          items:
            type: string
          description: List of tags to associate with ALL nodes.
        capabilities:
          type: array
          items:
            type: string
            enum: [s3, kvs]
          description: |
            Optional list of device capabilities to enable for ALL nodes.
            - "s3": Attaches DeviceFilePolicy for S3 file access via IoT Credential Provider.
            - "kvs": Attaches DeviceVideoPolicy for KVS streaming via IoT Credential Provider.
            If omitted, only the DefaultThingPolicy (MQTT/shadow) is attached.

    RegisterBulkNodesResponse:
      type: object
      required:
        - request_id
        - message
      properties:
        request_id:
          type: string
          description: The unique ID used to track this bulk operation.
        message:
          type: string

    BulkUpdateNodesRequest:
      type: object
      required: [cert_file_s3_path]
      properties:
        cert_file_s3_path:
          type: string
          description: |
            S3 path to the CSV file (must start with `s3://`). The CSV
            must contain a `node_id` column. A `certs` column is
            optional per row — non-empty triggers a cloud-side cert
            replacement for that node. Any other column is treated as a
            `column:value` tag, same convention as registration.
        admin_group_names:
          type: array
          items:
            type: string
          description: List of admin group names to add ALL nodes to.
        admin_parent_group_name:
          type: string
          description:
            Name of the parent group under which admin groups should be created.
        tags:
          type: array
          items:
            type: string
          description: List of tags (`key:value`) to apply to ALL nodes.

    BulkUpdateNodesResponse:
      type: object
      required:
        - status
        - request_id
      properties:
        status:
          type: string
          enum: [success, error]
        request_id:
          type: string
          description: The unique ID used to track this bulk operation.
        message:
          type: string

    NodeRegFailedNodeEntry:
      type: object
      properties:
        request_id:
          type: string
          description: ID of the bulk job this failure belongs to.
        node_id:
          type: string
          description: |
            Cert CommonName when the cert parsed successfully, otherwise
            the `node_id` column from the input CSV (the operator-
            supplied identifier, so the round trip to retry-csv is
            consistent).
        code:
          type: string
          enum: [DUPLICATE_NODEID, INVALID_CERT, SERVER_ERROR, UNKNOWN]
          description: |
            Coarse failure classification. Lets dashboards filter and
            bucket failures without parsing free text. Mapped from the
            error chain by `utils.ClassifyFailure` at the time the
            failure was recorded.
        reason:
          type: string
          description: |
            Full wrapped error chain joined with `": "` — captures the
            root cause, not just the top-level wrapper message.
        recorded_at:
          type: integer
          description: Unix seconds when the failure was recorded.
        expires_at:
          type: integer
          description: |
            TTL attribute. DynamoDB auto-deletes the row after this time
            (defaults to `recorded_at + 90 days`).

    ListFailedNodesResponse:
      type: object
      properties:
        request_id:
          type: string
        failed_nodes:
          type: array
          items:
            $ref: "#/components/schemas/NodeRegFailedNodeEntry"
        page_total:
          type: integer
          description: Number of rows in the current page.
        next_key:
          type: string
          description:
            Pagination token for the next page (absent if no more pages).

    GVARequest:
      type: object
      required:
        - requestId
        - inputs
      properties:
        requestId:
          type: string
          description: Unique identifier for the request
          example: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
        inputs:
          type: array
          items:
            type: object
            required:
              - intent
            properties:
              intent:
                type: string
                enum:
                  - "action.devices.SYNC"
                  - "action.devices.QUERY"
                  - "action.devices.EXECUTE"
                  - "action.devices.DISCONNECT"
                description: The intent type from Google Assistant
              payload:
                type: object
                additionalProperties: true
                description: Intent-specific payload data

    GVAResponse:
      type: object
      required:
        - requestId
        - payload
      properties:
        requestId:
          type: string
          description: Echo of the request ID from the request
          example: "ff36a3cc-ec34-11e6-b1a0-64510650abcf"
        payload:
          type: object
          additionalProperties: true
          description:
            Intent-specific response payload (SyncPayload, QueryPayload,
            ExecutePayload, or DisconnectPayload)

    GVACfgRequest:
      description: |
        Google Cloud service-account JSON for HomeGraph. Shape mirrors GCM
        (`PushGcmConfigRequest`) — both endpoints accept the same standard
        Google service-account key file. The full document is stored
        verbatim in SSM.
      allOf:
        - $ref: "#/components/schemas/GoogleServiceAccountConfig"

    GoogleServiceAccountConfig:
      type: object
      description: |
        Standard Google Cloud service-account key file. Used by both GCM
        (Firebase) and GVA (HomeGraph) integrations. Every field is
        required and stored verbatim.
      required:
        - type
        - project_id
        - private_key_id
        - private_key
        - client_email
        - client_id
        - auth_uri
        - token_uri
        - auth_provider_x509_cert_url
        - client_x509_cert_url
        - universe_domain
      properties:
        type:
          type: string
          enum: [service_account]
        project_id:
          type: string
        private_key_id:
          type: string
        private_key:
          type: string
        client_email:
          type: string
        client_id:
          type: string
        auth_uri:
          type: string
        token_uri:
          type: string
        auth_provider_x509_cert_url:
          type: string
        client_x509_cert_url:
          type: string
        universe_domain:
          type: string

    GoogleServiceAccountResponse:
      type: object
      description: |
        Read-side projection of `GoogleServiceAccountConfig`: every stored
        field except the write-only `private_key`, which never leaves the
        backend. `private_key_id` remains so admins can tell which key is
        configured.
      properties:
        type:
          type: string
          enum: [service_account]
        project_id:
          type: string
        private_key_id:
          type: string
        client_email:
          type: string
        client_id:
          type: string
        auth_uri:
          type: string
        token_uri:
          type: string
        auth_provider_x509_cert_url:
          type: string
        client_x509_cert_url:
          type: string
        universe_domain:
          type: string

    GVACfgResponse:
      type: object
      properties:
        project_id:
          type: string
          description: The stored project ID
        redirect_uris:
          type: array
          items:
            type: string
          description: Calculated OAuth redirect URIs
        message:
          type: string

    GVACfgGetResponse:
      description: |
        Stored service-account fields plus the calculated `redirect_uris`.
        The write-only `private_key` is never returned.
      allOf:
        - $ref: "#/components/schemas/GoogleServiceAccountResponse"
        - type: object
          properties:
            redirect_uris:
              type: array
              items:
                type: string

    GVACfgDeleteResponse:
      type: object
      properties:
        message:
          type: string
      required:
        - message

    NodeConfig:
      type: object
      description: |
        Full node configuration as reported by the device firmware. Mirrors
        the `NodeCfg` struct defined in the backend (data model, devices,
        services, and Matter endpoints).
      properties:
        data_model:
          type: string
          description: |
            "default" for RainMaker-native nodes, "matter" for Matter nodes.
          example: "default"
        devices:
          type: array
          items:
            $ref: "#/components/schemas/NodeConfigDevice"
        endpoints:
          type: object
          description: Matter endpoints, keyed by endpoint ID.
          additionalProperties:
            $ref: "#/components/schemas/MatterEndpoint"
        services:
          type: array
          items:
            $ref: "#/components/schemas/NodeConfigService"
        info:
          $ref: "#/components/schemas/NodeConfigInfo"
        message:
          type: string
      required:
        - info

    NodeConfigDevice:
      type: object
      required: [name, type, params]
      properties:
        name:
          type: string
        type:
          type: string
          description:
            "Device type (e.g., esp.device.switch, esp.device.lightbulb)."
        primary:
          type: string
          description: Name of the primary parameter.
        params:
          type: array
          items:
            $ref: "#/components/schemas/NodeConfigDeviceParam"
        attributes:
          type: array
          items:
            $ref: "#/components/schemas/NodeConfigAttr"
        message:
          type: string

    NodeConfigDeviceParam:
      type: object
      required: [name, type, data_type]
      properties:
        name:
          type: string
        type:
          type: string
          description: "Parameter type (e.g., esp.param.name, esp.param.power)."
        data_type:
          type: string
          description: "bool, int, float, string, or array."
        properties:
          type: array
          items:
            type: string
          description:
            "One or more of: read, write, persist, time_series, indexed."
        bounds:
          $ref: "#/components/schemas/NodeConfigParamBounds"
        ui_type:
          type: string
          description:
            "UI hint (e.g., esp.ui.slider, esp.ui.toggle, esp.ui.hidden)."
        message:
          type: string

    NodeConfigParamBounds:
      type: object
      properties:
        min:
          type: integer
        max:
          type: integer
        step:
          type: integer
        message:
          type: string

    NodeConfigAttr:
      type: object
      required: [name, value]
      properties:
        name:
          type: string
        value:
          description: Attribute value (any JSON type).
        message:
          type: string

    NodeConfigService:
      type: object
      required: [name, type]
      properties:
        name:
          type: string
        type:
          type: string
          description:
            "Service type (e.g., esp.service.time, esp.service.system)."
        primary:
          type: string
        params:
          type: array
          items:
            $ref: "#/components/schemas/NodeConfigDeviceParam"
        attributes:
          type: array
          items:
            $ref: "#/components/schemas/NodeConfigAttr"
        message:
          type: string

    NodeConfigInfo:
      type: object
      required: [fw_version]
      properties:
        name:
          type: string
        type:
          type: string
        fw_version:
          type: string
        model:
          type: string
        message:
          type: string

    MatterEndpoint:
      type: object
      required: [c]
      properties:
        dt:
          type: string
          description: Matter device type.
        c:
          $ref: "#/components/schemas/MatterEndpointClusters"
        message:
          type: string

    MatterEndpointClusters:
      type: object
      properties:
        s:
          type: object
          description: Server clusters, keyed by cluster ID.
          additionalProperties:
            $ref: "#/components/schemas/MatterServerCluster"
        c:
          type: object
          description: Client clusters, keyed by cluster ID.
          additionalProperties:
            $ref: "#/components/schemas/MatterClientCluster"
        message:
          type: string

    MatterServerCluster:
      type: object
      description: >-
        Matter server cluster. `i` (indexed) and `ts` (timeseries) are
        independent property sets and may overlap — an attribute that is both
        indexed and timeseries appears in both arrays. `v` holds config-only
        attribute values (reported in node config only, never in state). `a`
        lists the remaining plain attribute IDs and is omitted when empty.
      properties:
        a:
          type: array
          items:
            type: string
          description:
            Plain attribute IDs — not indexed, timeseries, or config-only.
            Omitted when empty.
        e:
          type: array
          items:
            type: string
          description: Event IDs.
        c:
          type: array
          items:
            type: string
          description: Accepted command IDs.
        i:
          type: array
          items:
            type: string
          description: Indexed attribute IDs.
        ts:
          type: array
          items:
            type: string
          description: Time-series attribute IDs.
        v:
          type: object
          additionalProperties: true
          description: >-
            Config-only attribute values, keyed by attribute ID. These
            attributes appear in node config only and are never reported in
            shadow params/state.
          example:
            "0x10": 42
        message:
          type: string

    MatterClientCluster:
      type: object
      properties:
        c:
          type: array
          items:
            type: string
          description: Generated command IDs.
        message:
          type: string

    NodeSchedulesPayload:
      type: object
      description: |
        The schedule service payload. Follows the ESP RainMaker built-in
        Schedules specification — this is the exact structure the device
        parses. The cloud stores and forwards it unchanged.
      properties:
        schedules:
          type: array
          items:
            $ref: "#/components/schemas/Schedule"
        message:
          type: string
      example:
        schedules:
          - id: "s1"
            name: "Morning Lights"
            enabled: true
            triggers:
              - m: 420
                d: 62
            action:
              Light:
                Power: true
                Brightness: 80
            validity:
              start: 1704067200
              end: 1735689600
          # Matter node: action mirrors the parameter-control payload —
          # endpoint -> c -> s -> cluster -> c -> { command hex: TLV hex }.
          # Here Endpoint 0x1, OnOff cluster 0x6, On command 0x1. A command
          # with no fields is the empty TLV struct, encoded as "0x1518".
          - id: "s2"
            name: "MatterMorning"
            enabled: true
            triggers:
              - m: 420
                d: 62
            action:
              "0x1":
                c:
                  s:
                    "0x6":
                      c:
                        "0x1": "0x1518"

    Schedule:
      type: object
      description: |
        A single schedule entry. The firmware uses `name` as an NVS key and
        requires it to be 16 characters or fewer. Additional keys (including
        `id`) are ignored by the device but stored by the cloud.
      required:
        - name
        - triggers
        - action
      properties:
        id:
          type: string
          description: |
            Cloud-side identifier. Opaque; the device ignores this field.
        name:
          type: string
          maxLength: 16
          description:
            Unique schedule name (also used as an NVS key on the device).
        enabled:
          type: boolean
          description: Whether the schedule is active.
        triggers:
          type: array
          description: One or more triggers that fire the schedule.
          items:
            $ref: "#/components/schemas/ScheduleTrigger"
        action:
          type: object
          additionalProperties: true
          description: |
            Node parameter-update payload applied when the schedule fires.
            Default data model: `{ <DeviceId>: { <ParamId>: <value>, ... }, ... }`.
            Matter data model: parameter-control payload
            `{ "0x<endpoint>": { "c": { "s": { "0x<cluster>": { "c": { "0x<command>": "<TLV hex>" } } } } } }`.
          example:
            Light:
              Power: true
              Brightness: 80
        validity:
          $ref: "#/components/schemas/ScheduleValidity"
        message:
          type: string

    ScheduleTrigger:
      type: object
      description: |
        Schedule trigger. The trigger kind is discriminated by which keys are
        present — there is no explicit `type` field:

        - **One-shot relative:** `rsec` (seconds after current time).
        - **Date-based:** `m` (minutes since midnight) plus at least one of
          `d` (day-of-week bitmask) / `dd` (day-of-month).
        - **Solar:** `lat` + `lon` plus exactly one of `sr` / `ss` (offset in
          minutes from sunrise/sunset). May also include `d`/`dd`/`mm`/`yy`.
      properties:
        rsec:
          type: integer
          description: Seconds after current time (one-shot relative trigger).
        m:
          type: integer
          minimum: 0
          maximum: 1439
          description: Minutes since midnight (date-based trigger).
        d:
          type: integer
          description: |
            Bitmask of days of week (bits 7..1 = Sun/Sat/.../Tue/Mon).
            Example: `5` (0b0000101) = Mon + Wed.
        dd:
          type: integer
          minimum: 1
          maximum: 31
          description: Day of the month.
        mm:
          type: integer
          description: |
            Bitmask of months (bits 12..1 = Dec/Nov/.../Feb/Jan).
            Example: `15` (0b000000001111) = Jan–Apr.
        yy:
          type: integer
          description: Specific calendar year.
        lat:
          type: number
          description: Latitude in decimal degrees (solar trigger).
        lon:
          type: number
          description: Longitude in decimal degrees (solar trigger).
        sr:
          type: integer
          description: Offset in minutes from sunrise (solar trigger).
        ss:
          type: integer
          description: Offset in minutes from sunset (solar trigger).
        message:
          type: string

    ScheduleValidity:
      type: object
      description: Optional validity window for the schedule.
      properties:
        start:
          type: integer
          format: int64
          description: Start of validity window, UNIX timestamp in seconds.
        end:
          type: integer
          format: int64
          description: End of validity window, UNIX timestamp in seconds.
        message:
          type: string

    NodeTriggerPayload:
      type: object
      required:
        - triggers
      properties:
        triggers:
          type: array
          description: |
            List of node-side triggers. Every entry must have a unique string `id`.
            `id` values are referenced from group automations in the form
            `<nodeID>~<automationID>~<triggerIndex>`.
          items:
            $ref: "#/components/schemas/NodeTrigger"
        message:
          type: string
      example:
        triggers:
          - id: "t1"
            type: "param"
            path: "Light.Power"
            operator: "=="
            value: true
          - id: "t2"
            type: "param"
            path: "0x1.c.s.0x6.a.0x0"
            operator: "=="
            value: true

    NodeTrigger:
      type: object
      required:
        - id
      description: |
        A node-side trigger definition. The server enforces only the presence
        and uniqueness of `id`; any additional fields are passed through to the
        device and interpreted by the firmware.
      properties:
        id:
          type: string
          description: Unique trigger identifier within this node.
        message:
          type: string
      additionalProperties: true

    AutomationStatus:
      type: string
      description: |
        Whether the automation is active. Disabled automations are stored but do
        not execute actions when their conditions are met.
      enum:
        - enabled
        - disabled
      default: enabled
      example: enabled

    Automation:
      type: object
      description: A single automation configured for a group.
      allOf:
        - type: object
          properties:
            id:
              type: string
              description: Automation identifier unique within the group.
              example: "a1b"
        - $ref: "#/components/schemas/AutomationPayload"
      example:
        id: "a1b"
        name: "Turn on porch light at sunset"
        description: "Triggered when ambient light falls below threshold"
        status: enabled
        conditions:
          and:
            - "nodeA~a1b~0"
        actions:
          targets:
            - node: "nodeB"
              path: "Light.Power"
              value: true
            - node: "nodeC"
              path: "0x1.c.s.0x6.a.0x0"
              value: true
            - node: "nodeC"
              path: "0x1.c.s.0x6.c.0x1"
              value: "150c000000"

    ListAutomationsResponse:
      type: object
      description: |
        Wrapper response for `GET /v1/groups/{groupId}/service/automations`.
        The `automations` array is always present; it is empty when no
        automations are configured for the group.
      required:
        - automations
      properties:
        automations:
          type: array
          items:
            $ref: "#/components/schemas/Automation"

    AutomationPayload:
      type: object
      description: |
        The user-supplied automation definition. Triggers referenced in
        `conditions.and` / `conditions.or` must exist as node triggers (see
        [NodeTriggerPayload](#components/schemas/NodeTriggerPayload)).
        Set `status` to `disabled` to pause the automation without deleting it.
      properties:
        status:
          $ref: "#/components/schemas/AutomationStatus"
        name:
          type: string
          description: Human-readable name of the automation.
        description:
          type: string
          description: Optional description of the automation.
        conditions:
          $ref: "#/components/schemas/AutomationConditions"
        actions:
          $ref: "#/components/schemas/AutomationActions"
      example:
        status: enabled
        name: "Turn on porch light at sunset"
        description: "Triggered when ambient light falls below threshold"
        conditions:
          and:
            - "nodeA~a1b~0"
        actions:
          targets:
            - node: "nodeB"
              path: "Light.Power"
              value: true
            - node: "nodeC"
              path: "0x1.c.s.0x6.a.0x0"
              value: true
            - node: "nodeC"
              path: "0x1.c.s.0x8.c.0x4"
              value: "042bff"

    AutomationConditions:
      type: object
      description: |
        Boolean combination of trigger IDs that fires the automation.
        Trigger IDs use the format `<nodeID>~<automationID>~<triggerIndex>`.
      properties:
        and:
          type: array
          items:
            type: string
          description:
            All listed trigger IDs must fire for the automation to run.
        or:
          type: array
          items:
            type: string
          description: Any one of the listed trigger IDs fires the automation.
        message:
          type: string

    AutomationActions:
      type: object
      properties:
        targets:
          type: array
          items:
            $ref: "#/components/schemas/AutomationActionTarget"
        message:
          type: string

    AutomationActionTarget:
      type: object
      description: |
        A single action performed when the automation fires — sets the data point
        at `path` under `node` to `value`.
      example:
        node: "nodeB"
        path: "Light.Power"
        value: true
      properties:
        node:
          type: string
          description:
            Target node ID. Used for cleanup when the node is removed.
        path:
          type: string
          description: |
            Within-node data point path.
            Default data model: `<device id>.<param id>` (e.g. `Light.Power`).
            Matter data model: the full dotted key chain into the desired shadow
            `0x<endpoint>.c.s.0x<cluster>.<a|c>.0x<attribute|command>`, where the
            `a`/`c` segment selects an attribute write or a command invoke
            (e.g. `0x1.c.s.0x6.a.0x0`, `0x1.c.s.0x6.c.0x1`).
        value:
          description: |
            Value to set for the parameter. Type depends on the parameter
            (boolean, number, string, or object).
          oneOf:
            - type: boolean
            - type: number
            - type: integer
            - type: string
            - type: object
              additionalProperties: true
        message:
          type: string

    AutomationWriteResponse:
      type: object
      properties:
        automation_id:
          type: string
          description: ID of the created or updated automation.
          example: "a1b"
        message:
          type: string
      required:
        - automation_id

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    NotFound:
      description: |
        Not found - the resource does not exist, or the feature is not enabled
        on this deployment. For username lookups the message is deliberately
        generic — it does not reveal whether an address is registered.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    Unauthorized:
      description: Unauthorized - Invalid or missing authentication
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    Forbidden:
      description:
        Forbidden - User does not have permission to access the resource
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    InternalServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

  securitySchemes:
    sigv4:
      type: apiKey
      name: Authorization
      in: header
      x-amazon-apigateway-authtype: awsSigv4
    CognitoAuthorizer:
      type: apiKey
      name: Authorization
      in: header
      description: |
        Bearer access token — ESP User OIDC for end users, admin Cognito pool
        for admins. Used only by `POST /v1/user/credentials`, which also needs
        the paired `id_token` in the body.
      x-amazon-apigateway-authtype: cognito_user_pools
    gvaAccessToken:
      type: apiKey
      name: Authorization
      in: header
      description: |
        ESP User `access_token` as `Bearer <access_token>`, issued to the
        voice-assistant client during Google account linking. Validated by the
        Lambda, not by an API Gateway authorizer.
    cognitoAuth:
      type: oauth2
      description: |
        Cognito User Pool OAuth2 with tiered scopes. Server-side authorization
        verifies the `custom:super_admin` claim for `super_admin`-scoped operations.
      flows:
        implicit:
          authorizationUrl: https://example.com/oauth2/authorize
          scopes:
            admin: Admin tier
            user: End-user tier

security:
  - sigv4: []
